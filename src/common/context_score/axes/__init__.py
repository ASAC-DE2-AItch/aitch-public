# =============================================================================
# axes/  —  축별 순수 서브함수 패키지 (패키지 마커)   Step4
# =============================================================================
# 각 축 함수는 "순수 함수"다: float 기여만 반환하고 부작용(0)이 없다.
# 파일이 축별로 분리되어 있어 A·B 가 서로 다른 파일만 편집 → 충돌 없음.
#
#   score_spc.py   [B]  룰 기본점·지속성·위반크기
#   score_tttm.py  [B]  score 가점·reference_suspect 감점
#   score_ae.py    [A]  drift 밴드·anomaly z
#   score_pred.py  [A]  P95 방향 가중항 (단독불가·Phase0~1=0)
#
# [공통 시그니처 원칙]  (계획서 §4-②)
#   def score_xxx(payload: dict, cfg) -> float   # 부작용 없음(순수)
#   combine 이 naive_sum → grouped_2stage 로 바뀌어도 이 시그니처는 불변.
#
# * 초기 세팅 단계에서는 비워 둔다.
# =============================================================================
