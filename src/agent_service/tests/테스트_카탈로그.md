# 테스트 카탈로그 (C4-1 ~ C5-3)

> **최종 갱신**: 2026-07-20 · *살아있는 문서 — 코드 바뀌면 같이 갱신(헌법 4-3)*
> `src/agent_service/tests/` 전체를 **이름 · 넣는 입력 · 검증** 으로 정리.
>
> ```powershell
> $env:PYTHONIOENCODING="utf-8"
> python -m pytest src/agent_service/tests/ -q -p no:langsmith_plugin   # 160 passed
> python -m pytest src/agent_service/tests/ -v -k "guard"               # 가드만 골라 보기
> ```
> ⚠️ `-p no:langsmith_plugin` 필수 — langsmith 플러그인이 xxhash DLL 정책에 막힌다(2026-07-16).
> parametrize 테스트는 fixture 수만큼 펼쳐져 개수가 늘어난다.
>
> **파일 5개**: alert_schema·pipeline_fanout(C4-1) / report_schema·llm_json(C4-2) / **supervisor(C5-3)**

---

## 1. `test_alert_schema.py` — AlertModel + fixtures (C4-1 Day1) · 66건
> alert 수신 스키마·게이트·관용 파싱 검증.

| 테스트 | 넣는 입력 | 검증 |
|---|---|---|
| `test_valid_fixtures_parse` *(×29)* | 정상 fixtures 29건 | 전부 `AlertModel` 로 파싱됨 |
| `test_exactly_one_broken_and_it_fails` | `*_BROKEN.json` | 파손은 정확히 1건, 그것만 `ValidationError` |
| `test_thirty_fixtures_total` | fixtures 폴더 | 총 30건 (DoD) |
| `test_gate_below_31_is_closed` | `01` (score 22) | `context_score==22`, `agent_gate_open==False` |
| `test_gate_at_31_is_open` | `02` (score 31) | `score==31`, `gate_open==True` (경계값 포함) |
| `test_max_severity_picks_critical` | `27` (N1 CRIT + N3 WARN) | `max_severity=="CRITICAL"` |
| `test_sensor_name_is_optional` | `01` 에서 sensor_name 제거 | 파싱됨, `sensor_name is None` (6-2 관용) |
| `test_chamber_id_rejects_deprecated_e14` | `chamber_id="E14"` | `ValidationError` (SIM_CH_ 패턴 위반) |
| `test_trigger_signal_optional_field_preserved` | `25` | 계약 밖 optional 필드 보존 |
| `test_shap_top3_are_distinct` *(×29)* | 정상 fixtures | `shap_top3` 센서 3개 중복 없음 |

---

## 2. `test_pipeline_fanout.py` — 관통 + tool 가드 (C4-1 ~ C5-2) · 51건

> alert 1건 → 게이트 → 3조수 병렬 → 리포트 3건. **C5-2 부터 3조수가 모두 LLM 경로**다.

### 2-1. 관통 · 게이트 · ID

| 테스트 | 넣는 입력 | 검증 |
|---|---|---|
| `test_gate_open_yields_exactly_three_reports` | `17` (score 84) | 리포트 3건 + 3종류 |
| `test_gate_edge_31_is_open` | `02` (score 31) | 3건 (경계값 포함) |
| `test_gate_closed_yields_zero_reports` | `01` (score 22) | `[]` — 기록만 |
| `test_gate_closed_does_not_call_llm` | `01` + 대본 0건 | LLM 미호출 (게이트의 존재 이유 = 비용·부하) |
| `test_report_id_format_and_prefixes` | `17` | `RCP/LIM/MNT-20260713-SIMCH5-0017` |
| `test_llm_report_id_is_overridden` | LLM이 지어낸 ID | 우리가 스탬프한 값으로 덮임 |
| `test_all_three_tools_call_llm` | `17` | **3조수 전부 LLM 호출** (C5-2 — 이전엔 recipe만) |
| `test_tools_reflect_their_own_llm_response` | tool별 대본 | 병렬 호출에서 **응답이 뒤섞이지 않음** |
| `test_recipe_falls_back_when_llm_unparseable` | 깨진 JSON ×9 | 3건 유지 + fallback (6-3 무중단) |
| `test_replay_skips_broken_and_parses_29` | fixtures 30건 | 파손 1 스킵 → 29건 (6-2) |
| `test_replay_end_to_end_report_count` | 29건 재생 | 총 리포트 == 통과 건수 × 3 |

### 2-2. recipe 가드 3종 — 숫자 → 손잡이 → delta

| 테스트 | 검증 |
|---|---|
| `test_number_guard_demotes_fabrication` | tuning 없는데 수치 냄 → 무효화·에스컬 (창작 금지) |
| `test_number_guard_corrects_tampered_value` | B 값과 다름 → **B 값으로 교정**(서술은 유지) |
| `test_number_guard_passes_faithful_citation` | 충실히 인용 → 통과 |
| `test_knob_guard_demotes_non_knob_sensor` | C17(측정값) 튜닝 시도 → `hypothesis=regime_signal` 강등 |
| `test_knob_guard_no_op_when_knob_map_empty` | knob_map 없음 → **강등 안 함** (판정 근거 없음, 오탐 방지) |
| `test_allowed_knobs_extracts_c_codes` | `'step_time(C41)'` 같은 혼재 표기에서 C코드만 추출 |
| `test_delta_guard_demotes_over_limit` | D6(±3%) 초과 → 튜닝안 제거 + escalate |
| `test_delta_guard_preserves_existing_escalate` | 기존 사유 보존하며 병기 |
| `test_stub_tuning_api_within_d6_and_none_when_no_violation` | 스텁이 D6 이내 · 위반 0이면 None |

### 2-3. limit 가드 3종 — 숫자 → 변동폭상한 → 가한계

| 테스트 | 검증 |
|---|---|
| `test_limit_number_guard_demotes_fabrication` | recalc 없는데 수치 냄 → 무효화 |
| `test_limit_number_guard_corrects_tampered_value` | recalc 값으로 교정 |
| `test_limit_reset_cap_guard_keeps_values_and_flags_approval` | **A2 초과 = 승인 전환**(수치 유지) ← recipe D6와 다름 |
| `test_limit_reset_cap_guard_no_op_without_delta_sigma` | σ 없으면 판정 불가 → 통과 |
| `test_limit_provisional_guard_blocks_during_phase` | `trigger_type='provisional'` → 재설정 보류 |
| `test_limit_provisional_guard_no_op_for_periodic` | 정기 재산정은 통과 |
| `test_limit_provisional_guard_no_op_when_unknown` | trigger_type 없으면 강등 안 함 |
| `test_limit_stub_recalc_stays_within_reset_cap` | 스텁이 A2(0.5σ) 이내 + `_provisional` 도장 |

### 2-4. maintenance 가드 2종 — 출처 → downtime

| 테스트 | 검증 |
|---|---|
| `test_manual_source_guard_demotes_when_no_material` | 재료 0건인데 조치 냄 → `action` 무효화 (§6 KB 미커버) |
| `test_manual_source_guard_passes_with_material` | 재료 있으면 통과 (문구 충실도는 프롬프트 소관) |
| `test_manual_downtime_guard_strips_unsourced_value` | 근거 없는 다운타임 제거 + 사유를 `uncertainty` 에 |
| `test_manual_downtime_guard_keeps_sourced_value` | 매뉴얼에 값 있으면 유지 |
| `test_manual_query_differs_from_case_query` | error_manual(증상+severity) ≠ historical_case(룰) |

### 2-5. 이동량 단위 σ 정본 (C5-2)

| 테스트 | 검증 |
|---|---|
| `test_recalc_stub_leaves_delta_pct_null` | B는 %를 안 냄 → 스텁도 창작 안 함, σ만 |
| `test_limit_number_guard_corrects_tampered_delta_sigma` | σ 변조 → recalc 값으로 교정 |
| `test_limit_number_guard_fills_omitted_sigma_when_numbers_present` | σ만 빠뜨리면 채움 (상한 근거 누락 방지) |
| `test_limit_report_carries_sigma_not_pct` | 관통 — σ 실리고 %는 **null 유지** (20% 오해 방지) |

### 2-6. KB 로그 페어링 (C5-2)

| 테스트 | 검증 |
|---|---|
| `test_incident_ids_extracts_and_dedupes` | 페어링 키 추출 + 중복 제거, 없는 사례는 조용히 제외 |
| `test_incident_ids_empty_inputs` | None/빈 입력 → 빈 리스트 |
| `test_pairing_logs_empty_without_incident_ids` | 키 없으면 **DB를 아예 안 침** |
| `test_pairing_logs_survive_db_failure_fast` | PG 미기동 시 **빨리 포기** (타임아웃 없으면 파이프라인이 멈춘다) |
| `test_limit_payload_carries_pairing_logs` | limit payload에 `limit_logs` 실림 |
| `test_recipe_payload_carries_pairing_logs` | recipe payload에 `recipe_logs` 실림 |
| `test_tool_kwargs_routes_logs_to_right_tool` | 서로 남의 로그를 안 받음 |

### 2-7. Qdrant 조회 · 검색어

| 테스트 | 검증 |
|---|---|
| `test_load_knob_map_returns_none_on_qdrant_failure` | Qdrant 실패 → None + 경고 (무중단) |
| `test_search_recipe_evidence_empty_on_qdrant_failure` | 검색 실패 → 빈 결과 |
| `test_kb_empty_counter_counts_search_failure` | [W16] 검색 실패 → 컬렉션별 0장 카운터 2건 |
| `test_kb_empty_counter_counts_successful_zero_hit` | [W16] **성공·히트 0** 도 계측 (종전 무흔적 경로) |
| `test_kb_empty_counter_counts_knob_map_loss` | [W16] knob_map 유실도 계측 (실패·카드0 양쪽) — 손잡이 가드 비활성 |
| `test_kb_empty_counter_silent_when_hits_exist` | [W16] 근거 있으면 카운터 비어 있음 (정상 경로 무잡음) |
| `test_build_queries_are_collection_specific` | 지식=짧게 / 사례=룰+서술 (2026-07-18 실측 반영) |
| `test_recipe_build_payload_carries_knob_map` | 재료가 payload에 실림 |
| `test_recipe_payload_carries_kb_hits_and_cases` | 근거 재료가 payload에 실림 |
| `test_recipe_uses_llm_response` | LLM 응답의 confidence·hypothesis가 리포트에 반영 |
| `test_recipe_prompt_carries_alert_tuning_and_knob_map` | 입력 구성 확인 (alert·tuning·knob_map) |

---

## 3. `test_report_schema.py` — Evidence Card 규칙 (C4-2) · 10건
> §5 근거 카드 규칙: 정상 통과 / 위반 거부. (`with pytest.raises` = 거부돼야 통과)

| 테스트 | 넣는 입력 | 검증 |
|---|---|---|
| `test_valid_card_parses` | 정상 카드(근거 3·출처 3·counter 有) | 통과, `len(evidence)==3` |
| `test_single_evidence_rejected` | 근거 1개 | 거부 (`min_length=2`) |
| `test_five_evidence_rejected` | 근거 5개 | 거부 (`max_length=4`) |
| `test_same_source_type_rejected` | 근거 전부 `spc` | 거부 (출처 2종 검증) |
| `test_two_distinct_source_types_ok` | `spc`+`case` | 통과 (경계값 2종) |
| `test_counter_evidence_required` | counter_evidence 제거 | 거부 (반증 필수) |
| `test_unknown_source_type_rejected` | `source_type="rumor"` | 거부 (`Literal` enum 밖) |
| `test_extra_field_forbidden` | `made_up` 필드 추가 | 거부 (`extra="forbid"`) |
| `test_report_accepts_evidence_cards` | 리포트에 카드 부착 | `EvidenceCard` 타입으로 승격 |
| `test_report_empty_cards_ok` | 빈 카드 리스트 | `[]` 도 정상 (`default_factory`) |

---

## 4. `test_llm_json.py` — LLM 엔진 (C4-2) · 13건
> `generate_structured` 흐름: 조립 · 성공 · 재시도 · fallback · 예산 분리. (`MockBackend` 대본 주입)

| 테스트 | 넣는 입력(대본) | 검증 |
|---|---|---|
| `test_build_messages_includes_system_block_and_example` | role+payload+example | §0 블록·툴 프롬프트·JSON 예시 포함 + 역할 분리 |
| `test_build_messages_retry_adds_suffix` | `retry=True`/`False` | 재시도 지시가 붙나/안 붙나 |
| `test_success_first_try` | `[정상]` | 첫판 성공, `calls==1` |
| `test_guided_json_is_model_schema` | `[정상]` | `guided_json` == 모델 스키마 |
| `test_generation_params_forwarded` | temp·seed·tokens | 생성 파라미터가 백엔드까지 전달 |
| `test_retry_then_success` | `[깨짐, 정상]` | 재시도 후 성공, `calls==2` |
| `test_validation_error_triggers_retry` | `[confidence 5.0, 정상]` | 스키마 위반도 재시도 |
| `test_parse_failures_return_fallback` | `[깨짐 ×3]` | fallback, `calls==3` (무한 아님) |
| `test_backend_exception_is_retried` | `[Timeout, 정상]` | 호출 실패도 재시도 → 성공 |
| `test_call_retries_are_separate_from_parse` | `[Timeout ×2, 정상]` | 호출 예산 별개 → fallback |
| `test_fallback_report_shape` | `make_fallback_report(...)` | `confidence 0`·`escalate_reason` 有 |
| `test_extract_json_strips_markdown_fence` | 코드펜스 감싼 JSON | 벗겨서 파싱 |
| `test_extract_json_plain` | 순수 JSON | 그대로 파싱 |

---

## 5. `test_supervisor.py` — Supervisor 판정 + 형식 가드 4종 (C5-3) · 20건

> tool 가드는 정답지(B 수치·KB)가 있어 "맞다/틀리다"를 판정했지만,
> **판정의 옳고 그름은 코드가 못 따진다.** 대신 **형식 정합**만 검사한다.

### 5-1. 가드 ① 실행가능 — 가드 우회 차단 (가장 중요)

| 테스트 | 검증 |
|---|---|
| `test_executable_guard_blocks_unactionable_option` | 조치 없는 옵션 선택 → `selected=None`. **verdict는 유지**(진단≠실행) |
| `test_executable_guard_allows_actionable_option` | 조치 있으면 통과 |
| `test_executable_guard_allows_approval_transfer_case` | **A2 "승인 전환"은 선택 가능** — 문자열이 아니라 **수치 유무**로 판정하는 이유 |
| `test_has_actionable_proposal_per_option_type` | 옵션마다 조치 자리가 다름 (value_proposed / center_after / action) |

### 5-2. 가드 ② verdict ↔ selected 정합

| 테스트 | 검증 |
|---|---|
| `test_verdict_option_guard_blocks_mismatch` | `baseline_aging` 인데 `manual_option` → 선택 무효화 |
| `test_verdict_option_guard_allows_match` | 3쌍 대응 전부 통과 |
| `test_verdict_option_guard_allows_none_selected` | `selected=None` 은 **항상 허용** (가드①이 만드는 상태) |

### 5-3. 가드 ③ 유령 인용

| 테스트 | 검증 |
|---|---|
| `test_citation_guard_masks_unknown_id` | 없는 ID → `[미확인 인용: …]` + 반증에 사유 병기. **개수 3 유지** |
| `test_citation_guard_passes_real_id` | 실존 ID는 그대로 |
| `test_citation_guard_skips_case_ids_without_cases` | 검증 근거 없으면 강등 안 함 (오탐 방지) |
| `test_citation_guard_catches_fake_report_id` | 리포트 ID는 cases 없이도 검증 가능 |
| `test_citation_guard_accepts_ids_from_all_evidence_sources` | **정답지는 재료 전체** — knob_map·KB 문서 ID 오탐 방지 (2026-07-20 실측 11건) |
| `test_citation_guard_still_catches_ghost_among_real_sources` | 재료를 다 줘도 진짜 유령은 잡음 |

### 5-4. 가드 ④ 기각 사유 · fallback · 관통

| 테스트 | 검증 |
|---|---|
| `test_rejection_reason_guard_fills_missing` | 안 고른 옵션에 사유 없으면 "미기재" 표지 (**창작 안 함**) |
| `test_fallback_brief_escalates_without_inventing_verdict` | 파싱 실패 → **판정 창작 없이 escalate**, conf 0.0, HOLD |
| `test_pipeline_yields_three_reports_and_one_brief` | **C5-3 DoD** — 리포트 3건 + Brief 1건 |
| `test_brief_report_id_is_stamped_not_llm_value` | report_id·incident_id·**chamber_id** 를 우리가 스탬프 (LLM이 다른 챔버를 우겨도 무시) |
| `test_gate_closed_yields_no_brief` | 게이트 미통과 → Supervisor 미호출 |
| `test_brief_falls_back_when_llm_unparseable` | 깨진 응답에도 파이프라인 생존 |
| `test_summarize_report_drops_evidence_cards` | Brief 입력은 요약 — 무거운 필드 제외 |

---

## 6. `test_crazy_marker_guard.py` — B9 crazy 마커 배제 (2026-07-28) · 12건

> B 확인요청(2026-07-28) → **ⓐ 소비 측 제외**로 합의. crazy alert 의 `violations[0]` 은 진짜
> 센서 위반이 아니라 **합성 마커**(`rule_id="B9"`·`sensor="C65"`)다 — 계약 §3 의
> `violations: min_length=1` 때문에 B 가 빈 칸을 채운 것. 이걸 대표 위반으로 쓰면 tool 3종이
> "C65 관리선을 (P99컷+0)/2 로 재산정" 같은 허구를 만든다.
> **게이트로는 못 막는다**: crazy 알람도 Nelson 과 같은 `context_score` 를 받는다(D-9 이관 원칙).
> ⚠️ 판별식 정본 `rule_id == "B9"` — orchestrator `incident_grouper.is_crazy_alert()` 와 동일 규칙.

| 테스트 | 넣는 입력 | 검증 |
|---|---|---|
| `test_marker_is_detected_and_excluded` | 마커가 **앞**에 온 혼재 alert | 마커만 배제, 대표는 진짜 위반(C62) — 순서 무관 |
| `test_crazy_only_has_no_primary_violation` | crazy-only alert | `violations` 는 있으나(계약 충족) 대표 위반 `None` |
| `test_limit_recalc_is_none_on_crazy_only` | crazy-only | recalc `None` — center 786·σ 262 역산 금지 |
| `test_limit_recalc_uses_real_violation_when_mixed` | 혼재 | `sensor_id=="C62"`, 진짜 관리선(1965/1635)에서 역산 |
| `test_limit_version_ignores_marker` | 혼재 | 마커 센서 조회 `None`, 진짜 위반은 `v1` 정상 |
| `test_limit_case_query_empty_on_crazy_only` | crazy-only | 사례 검색어 `""` |
| `test_recipe_tuning_is_none_on_crazy_only` | crazy-only | tuning `None` — 예측 결과값은 손잡이 아님 → 규칙6 |
| `test_recipe_tuning_uses_real_violation_when_mixed` | 혼재 | 손잡이 후보 C62, `value_current` 는 마커값 아님 |
| `test_recipe_queries_empty_on_crazy_only` | crazy-only | KB·사례 검색어 둘 다 `""` |
| `test_recipe_has_shift_ignores_marker` | crazy-only / 혼재 | 마커의 `CRITICAL` 만으로 급변 성립 금지 · 진짜 N1 은 유지 |
| `test_maintenance_queries_empty_on_crazy_only` | crazy-only | 매뉴얼·사례 검색어 둘 다 `""` |
| `test_maintenance_queries_use_real_violation_when_mixed` | 혼재 | 검색어가 C62 기준, `C65` 미포함 |

---

## 요약

| 파일 | 개수 | 무엇을 잠그나 |
|---|---|---|
| `test_alert_schema` | 66 | alert 수신 스키마·게이트·관용 파싱 (parametrize ×29 펼침) |
| `test_pipeline_fanout` | **51** | 관통 · **tool 가드 8종** · σ 정본 · KB 로그 페어링 · Qdrant 무중단 |
| `test_supervisor` | **20** | **Supervisor 형식 가드 4종** · fallback · 관통 DoD |
| `test_report_schema` | 10 | Evidence Card 규칙(개수·출처·counter·enum·extra) |
| `test_llm_json` | 13 | LLM 엔진(조립·성공·재시도·fallback·예산) |
| `test_crazy_marker_guard` | **12** | **B9 crazy 마커 배제** — tool 3종이 합성 마커를 대표 위반으로 쓰지 않음 |
| `test_release_brief` | **16** | **RTD 해제 근거 Brief**(#140) — Incident 단위 접기(장비 스코프 4행 → 1건) · 가드 3종(인용·근거 하한·확인 항목) · 신원/회고를 LLM 이 못 덮어씀 · fallback `not_ready` |

⚠️ **이 표는 2026-07-20 기준(160건)이고 위 12건만 덧붙인 상태다.** 이후 추가된
`test_disposition`·`test_knob_map`·`test_kafka_wiring`·`test_predictions_wiring` 은 아직
미반영 — 실측 합계는 **301건**(2026-07-28). 카탈로그 전체 갱신은 별건으로 처리한다.

### 이 카탈로그를 볼 때

- **"이 가드가 왜 이렇게 동작하지?"** → 테스트 **docstring 에 이유가 적혀 있다.**
  예: `test_executable_guard_allows_approval_transfer_case` 는 *왜 문자열이 아니라 수치로 판정하는지* 를 설명한다.
- 가드 전체도는 [tool_3종_구현맵](../notes/04_참조/tool_3종_구현맵.md),
  필드 의미는 [리포트_필드_사전](../notes/04_참조/리포트_필드_사전.md).
- **여기 없는 것**: LLM 판단 *품질* 은 pytest 로 못 잰다 → 평가 하네스 `eval_supervisor.py`
  (혼동행렬·드랍률·유령인용). 둘은 다른 도구다.
