# ⚠️ 파생 시나리오 (A26 · 프라이어 전이 원장 분산용) — 원본 = qual_loud_ch4.yaml
#   같은 값의 전이만 쌓이면 회귀는 점 1개라 마커 재캘리가 불가능하다(8/8 실측: 원장 2건 전부
#   동일값 → 후보 3종 적합 불가). PM 강도 스케일 s 하나만 곱한다 — ΔC17·ΔC11·ΔY 는 같은
#   실측 사건(C33 리셋 wafer 3692)의 세 얼굴이라 비율 보존이 재현이고, 새 물리를 만들지 않는다.
#   ΔY 앵커는 원본 주석의 실측 산포(+921~958) 안에서만 고른다 — 회귀 잔차가 발명한 노이즈가
#   아니라 기록된 측정 불확실성이 되게. ※ 이 세트의 개정 제안 지위 = '메커니즘 검증'.
#   ※ 시뮬 세계의 C17→Y 기울기(~ -24.9)는 실세계 앵커(-6.96)와 다르다 — 센서(N300 자)와
#   Y(초기 자)를 서로 다른 목적으로 고른 원본 선택의 파생값. 원장 재캘리가 이걸 찾아내는
#   것 자체가 서사다 ("다른 세계의 마커를 이 세계 자로 되찾는다").
# 시나리오 2 (M3 DoD): 대PM(가상) 후 "요란" 레짐 전환 — Qual 4지표 → 가한계 → 재산정 → CT②
# 실측 앵커: train_data C33 리셋 사건 (wafer idx 3692, 전후 100·300장 창 — 2026-07-20 측정 스크립트):
#   ΔC17 = -37.7 (N=300, wafer-mean 정착 / N=100은 -0.7 — 추세 혼입으로 창 민감. 방향은 하강 일관)
#   ΔC9  ≈ 0    (열 쌍 동행은 wafer 내 궤적 얘기 — 레짐 계단에선 침묵. 주입하지 않는 것이 재현이며,
#                C17만 움직여 C17·C9 관계가 깨지는 것 자체가 AE 반응의 실측 근거)
#   ΔC11 wf_min = +8.85 (+5.0~5.4σ, 창 무관 안정 — F14 방향 재현. 과도 행 한정 주입 → 정착 평균 침묵 유지)
#   ΔY   = +921~958 → +940 채택 (pre ~620 → post ~1,560)
#   ※ 구 인용 "+317"은 BL3 프라이어 추정치 계열로 추정 — 산정 창 대조 필요 (A 확인 항목, v5.2)
# 확장 후보 (주입 안 함 — 레짐 반응 실측 미검증): C32 과도, C54/C56 매칭 위치, C15/C16 가스 실측
scenario_id: SC2-QUAL-LOUD-CH4-S085
description: SIM_CH_4 가상 대PM 강도 s=0.85 — C17 -32.05 / C11 +7.52 / Y +814 (A26 원장 분산용 파생)
target_chamber: SIM_CH_4
pattern: pm_reset
start_after_wafers: 150          # 챔버 150장 정상 구간 후 개방 (스냅샷·baseline 확보)
level_shifts_eng:                # 정착 레벨 영구 계단 (공학단위)
  C17: -32.05
ignition_shifts_eng:             # 과도(stabilization_flag=1) 행 한정 (공학단위)
  C11: 7.52
y_shift: 814                     # fdc.actual 경유만 (헌법 1-3) · s=0.85 x 실측앵커 958
ground_truth:
  type: regime_transition
  verdict: loud                  # 기대 판정 = 요란 (Qual 4지표)
  correct_action: provisional_limits_then_requal_retrain   # 가한계 → Phase 2 재산정(R9) → CT②(AE)
  correct_disposition: HOLD_UNTIL_QUAL
  c65_effect: shift_plus_814
