# =============================================================================
# src/agent_a_mlops/  —  [담당: A] MLOps 파이프라인 (패키지 마커)
# =============================================================================
# 소유: 팀원 A (헌법 3-1 — 타 팀원 수정 시 A 리뷰 필수).
#
# 이 마커는 `src.agent_a_mlops.*` 임포트를 가능하게 해 **단위 테스트를 붙이기 위한**
# 것이다(기존엔 스크립트 실행 전용이라 테스트 불가). `consumer.py` 는 여전히
# `python src/agent_a_mlops/consumer.py` 로 직접 실행 가능하다.
#
# 무거운 의존(confluent_kafka·xgboost·pandas)이 없는 순수 계약 모듈은 별도 파일로
# 분리해 테스트 가능하게 유지한다 — 예: `shap_contract.py`.
# =============================================================================
