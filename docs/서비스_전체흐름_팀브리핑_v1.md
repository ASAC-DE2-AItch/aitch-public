# AITCH 전체 그림 — 팀 브리핑 v1.1 (2026-07-06 개정 — v4.5·일정/RnR v3 반영)

> 목적: 회의 전, 지금까지 확정된 **서비스 흐름·설계 근거·각자의 위치**를 한 문서로 공유.
> 상세는 각 문서 참조 (맨 아래 문서 지도). 이 문서는 "왜 이렇게 됐는지"의 요약본.

---

## 1. 우리가 만드는 것 (한 문단)

Etch 공정 FDC Trace를 실시간 수신해 **C65 불량을 예측**하고(A), **SPC/TTTM으로 이상을 감지**하며(B), 이상 발생 시 AI Agent가 **"진짜 장비 이상인가 / 공정 조건 이탈인가(Recipe) / 기준선이 낡은 건가 / 모두 아닌가"를 판별**해(C) 근거 리포트와 함께 **엔지니어 승인**을 받아 조치까지 연결하는 Human-in-the-Loop 제조 AI 플랫폼. **Recipe 변경은 승인 게이트를 거쳐야만 적용 가능 — 무승인 자동 적용 금지** (v4.5 — 멘토 정정으로 Recipe R2R 부활, 헌법 1-1).

핵심 차별점: 단순 이상 "감지"가 아니라 **판별 → 근거 → 승인 → 조치 → 검증까지 닫힌 루프**를 현업 절차 그대로 구현. 현업은 R2R을 자동(레거시 DB API)으로 돌리지만, 우리는 **설명 가능한 근거 + 승인 게이트**로 감사 추적을 확보한다 (멘토 확인 차별점).

---

## 2. E2E 서비스 흐름 (이것만 알면 됨)

```
[시뮬레이터 (PM)]  실데이터 리플레이 × 가상 챔버 6대 + 시나리오 주입 (답안지 sim_events는 채점 전용)
      │ fdc.raw ── A와 B가 각자 Consumer Group으로 병렬 구독
      ├────────────────────────────────┐
      ▼                                ▼
[A 추론 파이프라인] (병렬)         [B SPC 엔진] (병렬)
 XGBoost: C65 예측 + SHAP Top3      Nelson Rules(N1~N8): 센서가 선을 넘었나
 AE: anomaly/drift score            TTTM: 다른 챔버들과 다른가
 "다변량 규칙이 깨졌나"                  │
      │ fdc.prediction                 │
      └──────────────▶ B가 추가 구독 ──▶ Context Score 합산 (감지 3채널 수렴)
                                       │ fdc.alert  ←—— 유일한 경보 채널 (헌법 1-2)
                                       │              (AE는 직접 알람 권한 없음)
      ├────────────────────────┐
      ▼                        ▼
[PM Incident 백엔드]      [C 통합 Agent Service]
 같은 챔버+시간창 알람을     alert 수신 → 논리 Agent 3종 병렬 실행 (v4.5):
 Incident 1건으로 병합       ① Recipe Tuning 리포트 (수치=B API, 근거=Process KB)
 (동시 pending 1건)         ② 실력치 재설정 리포트 (수치는 B 엔진 API)
                            ③ 정비 리포트 (Error Manual RAG)
      │                        │
      │                        ▼
      │              [C Supervisor Agent]  4지선다 판정 + 옵션 순위 + Approval Brief
      │                        │
      ▼                        ▼
[PM LangGraph 승인 게이트]  interrupt() = Pending → 엔지니어가 대시보드에서
  승인 / 수정 승인 / 반려 / 에스컬레이션  (approval_records에 감사 기록 — RAG 자산으로 재활용)
      │ fdc.correction (승인 시, correction_type=limit|recipe)
      ├─→ [B] 관리 한계 즉시 갱신 (limit_version up)
      ├─→ [시뮬레이터] Recipe setpoint 반영 → 효과 검증 (v4.5)
      ├─→ 정비 조치 + wafer HOLD → SCRAP/RELEASE Disposition
      ▼
[모든 개방 후 Qual]  Qual wafer 5장(고정 표준 조건) → 4지표(AE·σ-갭·Nelson·C65 proxy) 판정 *(σ-갭 = 구 TTTM갭 대체, 2026-07-11 확정)*
      → 판정 승인 확정 = QualVerdictConfirmed 방송(fdc.agent) — A: is_post_loud_pm·bias 리셋 / B: 가한계 Phase / Dashboard
      ├─ 조용 → 복귀 (bias·라벨 연속, 스냅샷 갱신)
      └─ 요란 → 레짐 전환 Incident → 가한계 Phase 0~2 → (D+60 라벨) → R9 신규 수립 승인 → AE 재학습(CT②)
[Cycle 내]  Model R2R — "마지막 요란 판정 PM 이후" 라벨 잔차 rolling(N=500) → Bias 자동 보정 (상한 ±1×RMSE+기록, 초과=Incident 신호)
[매일] CT① — 일간 슬라이딩 재학습(창 365일 + 완결 요란 레짐 조항) → champion–challenger(레짐 홀드아웃) 통과 시에만 교체  (2026-07-22 재확정)
[LLM 확정 2026-07-10]  임베딩 bge-m3 hybrid · 생성 qwen3:30b-a3b · g5.xlarge 단일 (C 실측 벤치)
```

**대시보드(React, PM)**: 위 전 과정이 S0~S7 화면으로 노출. 시연은 시나리오 실행(S11) → 알람 → 승인 → 효과 확인 순.

---

## 3. 설계의 뼈대 — 멘토 확인으로 확정된 것들

| # | 질문했던 것 | 멘토 답 → 우리 반영 |
|---|---|---|
| 1 | 이상 대응 방식 | ~~Recipe 금지(v4.3)~~ → **멘토 정정(v4.5): Recipe R2R 포함** — 조치 4종: Recipe R2R / 실력치 재설정 / 정비 / Disposition (전부 승인 게이트, 무승인 자동만 금지) |
| 2 | TTTM 참조 기준 | golden chamber 지정 안 함 — **동일 공정 챔버 전체 분포(Box Plot) 비교** + 엔지니어 도메인 판단 → fleet median 정량화 + 자기 Qual 스냅샷 보조 |
| 3 | 관리 단위 | **Chamber × Recipe × Step** 확정. Window(과도/정착)는 관리 축이 아니라 지표 추출 계층 |
| 4 | 실력치 갱신 산식 | **이전 3개월 rolling mean ± 3σ, 자동 리캘리브레이션** — EWMA 폐기. λ 차등 이슈 소멸 |
| 5 | 현업 이상 대응 절차 | RTD 배차 제외 → BM 정비 → Sample wafer → 이전 실력치로 계측 — **우리 inhibit → 정비 Agent → Qual 5장 → 4지표 판정과 1:1 대응** |

**실력치 갱신은 3트랙** (4번 반영의 결과):

| 트랙 | 트리거 | 승인 |
|---|---|---|
| 정기 리캘리브레이션 | 주기적, 무이상 | **자동** (폭 상한 0.5σ 이내 + 이력 기록 조건 — 헌법 1-1 예외) |
| 이상 대응 재설정 | Incident | **승인 필수** |
| Qual 트리거 재산정 | PM/BM 후 Qual 통과 | **승인 필수** |

세 트랙 모두 같은 산식(rolling mean±3σ), 같은 테이블(`limit_corrections`, trigger_type으로 구분).

범위 밖으로 확정: 골든패스(장비 조합 최적화), dedication, n대화(양산 전개) — 스케줄링/램프업 도메인. 우리 멀티챔버는 "n대화 이후" 상태를 가정.

---

## 4. 4인 체제 — 누가 뭘 (요약)

| 담당 | 핵심 소유물 | 한 줄 요약 |
|---|---|---|
| **PM 윤준호** | 시뮬레이터, LangGraph 승인 인프라, Incident 백엔드, React 대시보드, 문서/계약 | 판이랑 심판 |
| **A 팀원 A** | 추론 파이프라인, AE, CT 2종, Qual 4지표 취합 | 예측하고 재학습 |
| **B 팀원 B** | Nelson/TTTM/Context Score, **실력치 엔진**(재산정+한계 갱신) | 감지하고 기준선 관리 |
| **C 팀원 C** | RAG KB 5타입(벡터 3컬렉션 + PG 이력 — 2026-07-07), 통합 Agent(리포트 3종 병렬 — Recipe·실력치·정비), **Supervisor 판단 로직(4지선다)** | 해석하고 추천 |

경계 원칙: C의 실력치·Recipe 리포트 **수치는 B의 엔진 API를 호출** (C는 근거+리포트만 — '정량은 B, 근거는 C'). Supervisor **판단은 C**, 승인 **게이트는 PM**. 상세: `팀_RnR_정의서_v3.md`, task 단위 일정: `일정_v3_task단위.md` (현재 W3, Code Freeze 8/13, 발표 8/19).

---

## 5. 절대 규칙 5개 (PR 차단 사유 — 헌법 요약)

1. **Recipe·실력치·정비·Disposition은 승인 필수** (예외 2개뿐: 정기 리캘리브레이션·Model R2R — 상한 내+기록 조건 자동)
2. **Recipe 무승인 자동 적용 금지** — 튜닝안 생성은 가능, 적용은 승인 게이트 통과 후에만 (v4.5)
3. **데이터 누수 금지** — C65를 feature로 사용 금지, 분할은 Wafer ID 그룹 기준
4. **fdc.alert 단일 채널** + 승인은 Incident당 **동시 pending 1건** (알람당 승인 생성 금지 / 순차 승인은 여러 번 가능 — 헌법 1-4 P3)
5. **센서는 C코드 그대로** (`C11`) — 발표명(DC_Bias)은 화면에서만. `sim_events` 조회 금지(답안지)

---

## 6. 문서 지도

| 궁금한 것 | 문서 |
|---|---|
| 전체 설계·근거 | `능동형_제조_AI_플랫폼_기획서_v4.5.md` (마스터) |
| 내 모듈의 입출력 JSON | `API_Contract_명세서.md` — **이것만 믿고 병렬 개발 시작** |
| 컬럼이 뭔지 (C11이 뭐야?) | `데이터_사전_v1.md` (v3.1) |
| DB 테이블/네이밍 | `db/init.sql` + `DB_네이밍_컨벤션_v1.md` (예제 하나로 90%) |
| 내 task와 마감 | `일정_v3_task단위.md` |
| 화면이 어떻게 생겼나 | `프론트_화면_정의서_v1.md` (S0~S11) |
| 시뮬레이터가 뭘 주나 | `시뮬레이터_스펙_v1.md` |
| 규칙 전문 | `CLAUDE.md` (헌법 — 코드 리뷰 기준) |

---

## 7. 오늘 회의에서 정할 것

`config_파라미터_합의안_v1.md`의 파라미터 약 40개 ✅/수정 표기 (30분 목표) + **B5-4(Recipe 정량 엔진) B 동의** + **AE 배치 단위(공용 1개+챔버 정규화) A 확정**.
특히 시선 필요: **A4** rolling window N=500 wafer (3개월의 시뮬 환산 — B 의견), **B5** Incident 병합창 20 wafer (PM), **C2** Qual 4지표 기준값 (A).
합의 즉시 PM이 `config/params.yaml` 생성 → 이후 모든 모듈은 이 파일만 참조 (하드코딩 금지).
