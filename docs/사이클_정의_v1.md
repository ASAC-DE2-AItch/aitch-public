# PM 사이클 정의 v1 — 데이터 실측 기반 (2026-07-08 기획 마감 · 세계관 확정)

> 작성: PM 윤준호. 시뮬레이터·CT·Model R2R·실력치·Qual 로직이 공유하는 **사이클 세계관의 단일 소스**.
> **개정 이력**: 2026-07-07 팀 리뷰 프레임 확정 → 2026-07-08 회의 승인 후 세부 확정 (Qual 실행 스펙·판정 기반 원칙·F14 C11 이중성격·F15 hour 실측·F16 레시피 손잡이·F17 라벨-프리 프라이어·처리율 231 정정·is_post_loud_pm rename·신규 결정 9·10).
> **🔒 기획 마감 (2026-07-08)**: 이 문서 기준으로 사이클 세계관을 **동결**한다. 이후 변경은 평시 금지 — **문제 발견 시에만 Incident 방식으로 재오픈**(수정안 → PM 승인 → 반영). 실행(빌드) 단계는 이 문서를 읽기 전용 단일 소스로 참조. 미결 항목은 §5 보류 칸 참조 (외부 확정 도착 시 끼워넣기).
> 원칙(멘토): "설득력만 있으면 우리가 정하는 게 맞다" — 모든 정의에 근거 등급 표기:
> **[실측]** 우리 데이터 / **[문헌]** 외부 / **[세계관]** 우리가 정한 가정 (config 조정 가능)

---

## 1. 실측 사실 (scripts: analyze_pm_cycle · check_seg1_tail · check_seg2_c23 · check_regime_sensors · check_qual_backtest · check_c12_and_downtime · verify_deployment_bias · check_c11_extremes · check_hour_effect · check_c17_prior · check_prior_ensemble(v1·v2) · check_transient_tracker)

| # | 사실 |
|---|---|
| F1 | 3분할은 동일 기간(11/30~2/8)의 wafer 랜덤 분할. **전체 15,919장/69일 → 처리율 231장/일** (train 단독 170/일은 과소) |
| F2 | C33(RF time) 증가율 **1.55~1.57/일로 일정** — 가동률 일정한 usage 시계. 관측 리셋 1회(12/23) |
| F3 | seg1은 C33=3 시작 → **~25일 완결 사이클**. seg2는 C33 1→74, **47일+ 미완**. 개방 간격 표본: 25일 · ≥47일 |
| F4 | **리셋 직전 상태 양호** (말기 C65 −0.82σ 개선) → 12/23 개방은 상태 반응이 아닌 **정기(수명 기반)** |
| F5 | **배치(C23)·레시피(C6)·정착(C42=0) 통제 후에도** seg1 639 → seg2 1,146 — **+507 지속. PM이 원인** (캠페인 아님) |
| F6 | PM 직후 과도는 **지수 감쇠** (1,418→…→998, 감쇠폭 축소·점근 ~1,000 수렴, 1,000+장) |
| F7 | C33↔C65 상관 −0.013 — Y는 위상이 아니라 **PM 이벤트(계단)**에 반응 |
| F8 | 배포 실험(seg1 학습→seg2): 평균 잔차 **+489**. 이전 레짐 잔차 전이 무효(605→606), 당 레짐 rolling bias 유효(지연 0에서 85) |
| F9 | **PM 효과 이질적** — seg1도 자기 PM 직후(~460장 시점부터)인데 평온. **"조용" 결과 표본 ①**(직전 개방 — 작업 유형 미상) / **"요란" 결과 표본 ②**(12/23 — 17h·대형 shift로 대PM 추정). ※ 실측으로 아는 건 유형이 아니라 **결과**뿐 — 대응을 판정 기반으로 거는 근거 |
| F10 | **레짐이 센서에 새겨짐**: **C17 −4.5σ**(온도 레짐 — Y상관 −0.8, 인과 후보), C12 +점프(제어 기준값 갱신). C11·C62 등 7종 침묵 — **정착 평균 기준** (C11은 wafer 최솟값이 반응: F14) |
| F11 | **레시피 실험 실존**: C6_1 93장(12/31~1/1) — C65 990 vs 주변 1,233 (**−240**) 후 원복 → **Recipe R2R 실데이터 증거** |
| F12 | **Qual 판정식 백테스트 통과**: 기준산포=직전 사이클 전체·C12 제외·3σ·5장 → PM직후 3.01σ(요란)/초반 0.82σ/말기 2.87σ(조용). ⚠주의: 좁은 창 σ 기준은 C17 유동으로 오판(8σ대) — 산포는 반드시 사이클 전체로 |
| F13 | **대PM 다운타임 17.0시간 [실측]** (12/22 23:32 → 12/23 16:31). 달력 결측 2일(12/6·1/30)은 리셋 무관. 17h는 세정+킷 교체+캘리브레이션 복합 패키지의 방증 |
| F14 | **C11 이중 성격 [실측]** *(2026-07-08)*: 정착·과도 **평균**은 레짐 침묵(0.13σ/0.07σ)인데 **wafer 최솟값(점화 스파이크 최심점)은 +6.56σ 반응** (−347.1→−336.1, seg1 내 σ 1.67 — 하드웨어 지문 수준으로 일정). 결합 모델의 "C11=레짐 프록시(0.81)·C11_wf_min gain 최상위"와 우리 "C11 침묵"은 **표현 차이 — 둘 다 사실**. 최댓값은 −1 고정(포화 — C11_wf_max 피처는 상수, 정리 후보). 판정식(정착 평균 기준)엔 영향 없음 |
| F15 | **hour(시각) 효과 [실측]** *(2026-07-08 검증)*: 결합 모델에서 hour 피처가 valid RMSE 55.97→51.88 (**−4.1**). **날짜·배치 통제 후에도 잔존** — 진폭 41.3, 시간대 산포/표본오차 **3.8배**. 교대 집계: **야간(22-06) +7.0 열세 / 스윙(14-22) −5.0 우세** / 주간 −1.8. 스케줄 그림자 아닌 실효 패턴. 프로파일 → `config/hour_profile_v1.csv` |
| F16 | **레시피 손잡이 실측 [팀 0b 분석]** *(2026-07-08)*: C6_1(93WF, 시간창 통제 불량 −22%)에서 실제 바뀐 파라미터 = **C12 Vdc setpoint(+5.48SD, 완전 분리)·C41 식각시간(2배)·C31 파워(하향)·C62·가스 계열 등 12+개 동시(완전 교락)** → per-knob 인과는 데이터로 특정 불가, 레시피 정의서/도메인 필요(멘토 Q5). **멘토 확인 (2026-07-09)**: 가변 가능 para = **temp·gas유량·pressure**(+파워·시간), **temp 1순위**. "temp 하나 가변 시 EPD·장비 상태 para도 연동 변화" → 12+개 동시 변화의 정상 원인이자 손잡이 후보 압축(의도 1~2 vs 연동 결과)의 현업 근거. **함의**: R2R 튜닝안 어휘는 모델 피처가 아니라 **레시피 파라미터 층**(Process KB 매핑) — SHAP는 축(온도/플라즈마)까지, 손잡이 선택은 KB. C12는 판정에선 제외(도장)·레시피 층에선 핵심 파라미터 — 층별 역할 분리. **교락은 "지금 풀고 시작"이 아니라 "루프로 푼다"**: ① R2R은 원래 크기를 한 번에 아는 시스템이 아니라 방향(KB·물리) 맞는 보수 스텝(D6 명목 대비 누적 ±3% — 1회당 아님, ≈3스텝 후 에스컬) → 검증 → 재조정으로 수렴하는 루프 — 크기는 입력이 아니라 출력. ② 관찰 데이터 통계로 완전 교락은 불가(정직) → Agent가 **split-run 실험(DoE)까지 설계·제안**, 시뮬 세계라 제안→승인→적용→채점이 실제 순환하며 (손잡이,효과) 원장 1점씩 축적 = 교락 점진 분해 + 검증된 변형이 새 패키지로 편입(패키지 2개 한계 자가 해소). ③ 도메인(멘토 Q5 레시피 정의서)이 "의도 변경 1~2 vs 부수 효과 10"을 가르면 교락 절반 선해소. **HITL 5단계 중 사람은 "선택·승인" 1단계만** (진단·후보압축·사례정량·효과검증은 시스템) |
| F17 | **라벨-프리 프라이어 백테스트 [실측]** *(2026-07-08)*: D+0 Qual의 C17 델타(−52.3)로 Y 레벨 추정 — ① seg1 내부 기울기(순환 없음)는 **실패**(11배 과소: 레짐 내 미세변동 ≠ 레짐 간 이동), ② 전이 앵커 기울기(−6.96, Qual 기준 캘리브레이션)는 **방향 ✓**. 3층 커버리지: 무보정 486 → 프라이어 141 → **+감쇠 템플릿 64**(초반 14일 681→79, 템플릿 τ=12일·점근 1004 vs 실측 1001). 다채널: **C17 주채널 유지**(백업 C12·C11_min은 읽기 σ 낮으나 인과 아님·확인용), 교차확인 3σ_gap 오경보 0.2%·대형 고장 탐지 99%+·백업 생존 오차 14. 과도 추적 센서 부재 확인. 스크립트: check_c17_prior·check_prior_ensemble(v1·v2)·check_transient_tracker |

**⚠️ 파생 경고 (A 팀원 A)**: EDA "C23이 Y 지배"는 상당 부분 **세그먼트(PM) 효과의 교락** (같은 배치가 PM 전후 +750 이동). **C23 피처 = 세그먼트 프록시(누수 유사) 위험.** C12·C17은 EDA 핵심 6피처 — v1 최종 피처셋 잔존 확인 요청.

---

## 2. 용어·세계관

| 용어 | 정의 | 등급 |
|---|---|---|
| **사이클** | C33 리셋 ~ 다음 리셋 (챔버 개방 간격) | [실측] |
| **소정비 (minor)** | 세정 중심 개방 (킷 교체 없음) — 결과는 사실상 항상 **조용** (건강 센서 침묵; C12 등 제어 기준값은 갱신될 수 있음) | [세계관 — 전/후 쌍 표본 0. 유형은 생성·기록용 라벨] |
| **대PM (major)** | 대형 킷 교체 **포함** 개방 — 소정비 작업을 **무조건 포함**(세정은 킷 교체의 선행 작업이라 "세정 없는 대PM"은 물리적으로 성립 안 함; 17h 실측 = 복합 패키지의 방증). 결과는 강도 분포: **~70% 조용 / ~30% 요란**(C17류 레짐 이동 + Y 점프 + 과도 감쇠) | 요란 서명 [실측 F5·F6·F10] / 주기 [세계관 — 멘토 3~4개월] / 유형은 생성·기록용 라벨 |
| **BM** | 무계획 고장 — PM 무관 랜덤, sudden/gradual 2형 | [문헌 PHM 2018] |
| 캠페인(C23) | 배치 축(28종, 중앙값 41일) — 시뮬 재현, Y 지배력은 PM이 주인 | [실측] |

> **용어 주의 — 두 축은 동의어가 아님**: 소정비/대PM = "무슨 **작업**"(유형 — 기록되는 사실) / 조용/요란 = "레짐이 **바뀌었나**"(판정 — Qual이 결정). 관계: 소정비→사실상 항상 조용, 대PM→~70% 조용·~30% 요란. 즉 **"조용" = 소정비 전부 + 대PM의 70%**. 시스템 대응은 판정 축만 사용(원칙), 유형 축은 생성·기록·설명용.
>
> | | 조용 | 요란 |
> |---|---|---|
> | 소정비 | 정상 케이스 | (세계관상 없음) |
> | 대PM | ~70% | ~30% ← 12/23 표본 |

**조용/요란 판정 (레짐 경계 스위치)** — 사전 판별은 원리적 불가. **모든 개방 후 Qual 5장** 수행(소정비 포함), **직전 유효 스냅샷**과 비교해 판정 (유효 스냅샷 = 조용 판정 시 그 Qual로 갱신된 것 / 요란 이후엔 Phase 2 확정본 — R5):
- **판정식 [F12 백테스트 통과]**: 건강 센서 8종(C12 제외 — 제어 기준값이지 건강 신호 아님)의 σ-갭, **기준 산포 = 직전 사이클 전체 wafer 산포**, 임계 **3σ**. Qual 4지표(AE·σ-갭·Nelson·C65) 종합으로 판정하되 — **운영 실효 주력은 σ-갭 + AE 두 축** (둘 다 라벨 불요: C17 이동은 σ-갭이, 새 레짐 파형은 옛 레짐을 학습한 AE가 잡음). **C65 지표는 예측값(Proxy Metrology — Qual은 WT 미경유라 실측 없음)이라 과도기엔 기여 없음** (옛 모델의 예측은 낮게 나와 태연히 정상으로 보임). ⚠ F12 백테스트의 "C65 FAIL"은 실측 기준이었음 — 운영과 다름을 명시
- 산포 계산 시 **과도 구간(A7 제외 구간과 동일)은 제외** — 직전 사이클이 요란 레짐이면 과도가 산포를 부풀려 다음 판정이 둔감해짐 (R1)
- **부트스트랩**: 운영/시뮬 개시 시점의 초기 스냅샷·기준 산포는 **seg1 실측으로 시딩** (B3-1 초기 실력치 테이블과 동일 소스 — R3)
- **스냅샷 리베이스 (R5)**: 조용 판정 시 그 Qual로 스냅샷 즉시 갱신 / 요란 판정 시엔 **Phase 2 정식 재산정 때 새 레짐 스냅샷·기준 산포로 확정** — 리베이스 없으면 다음 소정비가 옛 레짐 스냅샷과 비교돼 **영구 '요란' 오판 루프**(bias 차단 연쇄). 장기 점진 표류 감시는 이 축이 아니라 SPC 추세 룰 + 리캘리 누적 상한(A3) 소관. *(검증 상태 주의: "조용" 판정 사례는 F12 백테스트에서 2건 확인(0.82σ·2.87σ) — 단 이는 "개방 직후 5장" 조건의 근사 표본이며, 정확한 조용-개방 전/후 쌍 실측은 없음(한계 ⑤). 조용 경로의 최종 검증은 시뮬 리허설에서)*
- **요란 ≠ 불량**: 요란은 "레짐 전환 감지"이지 불량 판정이 아님 (개선 방향 전환일 수도) — 후속은 자동 재정비가 아니라 **엔지니어 분기(S7: 복귀+가한계 / 추가 Qual / 재정비)**
- **대응은 유형이 아니라 판정에 건다 (원칙)**: 시스템의 모든 후속 로직(가한계·bias 리셋·리베이스·A7·TTTM 유예)은 작업 유형(소정비/대PM)이 아니라 **판정 결과(조용/요란)**에만 걸린다. 유형은 정비 기록·시뮬 생성·Evidence/사례/Scorecard의 **맥락 데이터**로만 사용. 따라서 "대PM인데 조용"(강도 분포의 ~70%)이면 소정비처럼 취급하는 것이 **올바른 동작** — 편차가 없는데 가한계를 돌릴 이유가 없음. "대PM임을 시스템이 어떻게 아는가"라는 질문 자체가 불필요해짐 (정비 주체는 기록으로 알고, 시스템은 알 필요 없음)

**Qual 실행 스펙** *(2026-07-07 팀 회의 후 상세화)*:
```
언제:   모든 개방의 다운타임 종료 직후, 생산 재개 전 (소정비 포함)
발행:   시뮬레이터 QUAL 모드 (P5-3) — is_qual=true wafer 5장 연속
조건:   고정 표준 조건 (R7): 표준 레시피 C6_0 · 배치 영향 배제 ·
        센서 트레이스 = "현재 챔버 상태(레짐 운전점+오프셋)의 정착 구간 리샘플"
        → 무(無)에서 창조가 아니라 조건 통제된 리샘플 — 실측 앵커 유지
처리:   일반 wafer와 동일 파이프라인 (Qual 전용 코드 경로 없음 — 기획서 원칙)
        · B: 센서 σ-갭(스냅샷 대비) + Nelson  · A: AE score + C65 예측(proxy)
취합:   A5-3이 4지표 통합 → quals 테이블(PENDING) → S7 → 엔지니어 판정
후속:   조용 → qual_snapshots 갱신(R5) / 요란 → S7 분기 → 복귀 시 가한계 진입
실측:   Qual wafer는 fdc.actual 미발행 (WT 미경유 — 현실 정합)
```
- 요란 → AE 재학습(CT②) + 가한계 모드 진입 + rolling bias 리셋 / 조용 → 복귀 (bias·라벨 연속)
- 잔여 위험(침묵-요란)은 운전 중 예측/AE → 실측(fdc.actual)이 순차 방어

**관측 데이터의 위치**:
```
[이전 대PM]──…──[소정비]══seg1(25일)══[대PM 12/23, 17h]══seg2(새 사이클 0~47일+)══(개방 예정)──…
 (미관측)         ↑관측 직전            ↑레짐 경계(요란)      ↑과도→정착 실측 보유
```

---

## 3. 시뮬레이터 생성 규칙 (최종)

| 규칙 | 값 | 등급 |
|---|---|---|
| 개방 간격 | **uniform(25, 50)일** | 하한 [실측 25 + 문헌 wet clean 2~4주] / 상한 [세계관 — seg2 무개방 47일과 모순 없는 최소치] |
| 대PM 승격 | 개방 시점에 직전 대PM부터 **누적 C33 ≥ 156 (≈100일 상당)이면 그 개방을 대PM으로 승격** (소정비 작업 흡수 수행) — 기준은 달력이 아니라 **RF 사용량(C33)**: 다운타임 중 정지와 정합, 수명 카운터 문법 (R8) | [세계관 — 현업 작업 패키지 관행 + 17h 실측] |
| 파생 통계 | 소정비 **2~3회/레짐 지배적** (1회는 <1% 엣지 허용 — 현업 2~5회 부합), 대PM 실효 주기 **~119일 ≈ 4개월** (범위 100~150일 — 임계 100 초과 후 "다음 개방"에서 실행되므로), **최소 사이클 100일 > 라벨 60일 → 모든 대PM 사이클에서 R2R 가동 보장** | 계산 |
| 레짐 리샘플 (대PM 시) | **새 운전점을 (C17류 센서, 정답 Y) 짝으로 함께 샘플** — 실측 2점((218,638)·(160,1123))·상관 −0.8 앵커, 70% 소변화/30% 대변화, **레벨 재추첨(mean-reverting — 누적 폭주 방지)**. Y는 생성 시 확정, fdc.actual로 60일 후 공개 | [실측 앵커 + 세계관] — 센서 동반 필수 (아니면 시뮬 세계에서 AE·Qual 감지 서사 붕괴) |
| 과도 | 지수 감쇠 fit — 꼬리는 **같은 곡선의 연장(기울기 0 수렴)**, 평탄선 삽입 아님 | [실측 F6] |
| 사이클 말기 | **평온 기본** [실측 F4] — 열화·이상은 전부 시나리오 주입기 소관 (답안지 일치 → 채점 성립) | 설계 원칙 |
| 다운타임 | **대PM 12~24h (중심 17h [실측 F13]) / 소정비 6~10h (중심 8h [문헌])** — 작업 범위 변동 반영, shift 강도와 느슨히 연동 가능 (config) | — |
| 시간대 패턴 (hour) | Y에 **hour(0~23) 프로파일 오프셋** 부여 — 실측 프로파일 산출 완료(`config/hour_profile_v1.csv`: 진폭 41.3, 야간 +7/스윙 −5), 진폭 스케일 config. 재현 안 하면 결합 모델의 hour 피처가 시연 세계에서 죽은 피처가 됨. **⚠ 적용 시점 = 합성 Y 생성기 도입 시에만.** 현재 시뮬은 실데이터 리플레이(C65 원본 그대로)라 hour 효과가 이미 포함 → 지금 프로파일을 더하면 이중 계상. 프로파일은 Y를 0에서 조립하는 합성 모드(레짐 레벨+배치+hour+노이즈)의 재료로만 소비 | [실측 F15 — 날짜·배치 통제 후 잔존 3.8배 확인, 2026-07-08 결정] |
| 내부 데이터 | 레짐별 실측 세그먼트 위상 리샘플 + 챔버 오프셋(E3) · Y 생성 = 레짐 레벨 + 배치 소폭 오프셋 + wafer 노이즈 · 캠페인(C23) 스케줄 재현 · BM 포아송 [문헌] · 처리율 231장/일 [F1 — 다운타임 포함 평균이므로 가동일 발행률은 소폭 상향 보정] | — |
| 답안지 어휘 | sim_events.correct_action에 **`requalify`(신규 기준선 수립)** 추가 — 요란 PM 시나리오의 정답 라우팅(가한계+CT②) 채점용 (R4) | — |
| Qual 발행 | 모든 개방 후 is_qual wafer 5장 — **고정 표준 조건** (배치·레시피 영향 배제: 판정 σ-갭이 배치 잡음에 오염되지 않도록, R7) | — |

> **현업 예외의 취급 (2026-07-22 명시 — PM)**: 수급·물량에 의한 킷 교체 연기, 사고성(BM) 조기 대PM 등 현업 예외는 **배경 생성 규칙(R8 결정론)에서 의도적으로 제외** — 결정론이 파생 통계와 "최소 사이클 100일 > 라벨 60일 → R2R 가동 보장"의 안정 조건이다. 예외가 필요한 데모·검증은 **시나리오 계층에서 통제 주입**한다 (조기 개방은 S7 재정비·정비 승인(MNT) 경로가 이미 담당). 시스템 대응은 유형이 아닌 **Qual 판정 축**이므로, 세계가 배경 규칙을 벗어나도 대응 로직은 무관하게 성립한다.

---

## 4. 운영 로직 정합

```
D+0     개방 (C33 리셋, pm_count+1) — 소정비든 대PM이든:
        Qual 5장 → 4지표 종합 판정
        ├─ 조용 → 복귀 (bias·라벨 연속, 실력치 유지)
        └─ 요란 → 엔지니어 분기(S7: 복귀/추가 Qual/재정비) → 복귀 시 가한계 모드 진입
              + rolling bias 리셋·이전 라벨 차단 (CT② 정식 재학습은 Phase 2 예약 — R6)
D+0~4   (요란 시) 과도 ~1,000장: 예측은 옛 레짐 출신이라 크게 틀림 — 방어는 감지층
        (C17 이동을 AE/N5가 봄; predicted 기반 crazy는 무력하지만 B9의 anomaly OR 조항이 커버)
        Incident → 리포트 3종 → 4지선다 (실측 선례: 레시피 실험 = Recipe R2R 경로 — F11)
D+4~60  가한계 SPC + AE + 보수 운전 (예측 공백은 구조적 한계로 명시)
D+60~   fdc.actual 도착 — "마지막 요란 PM 이후" 라벨만 rolling bias에 사용
        (자동 적용 최소 표본 50 — SE=σ/√N≈10.7 근거 / 상한 ±1×RMSE clamp)
D+62~   당 레짐 라벨 ≥500 축적 (SE≈3.4) → **"신규 수립" 승인 1회 → 대형 오프셋 적용 (R9)**
        → 예측 정상화 (미수립 시 사이클 종료까지 ~430 잔차 방치되던 구간 해소)
CT① 재학습 = 일간 슬라이딩 (매일 재학습·가장 오래된 하루 폐기) — champion–challenger 통과 시에만 배포
        window = 365일 (현업 대PM 3~4개월 × 최악 3사이클) + 완결 요란 레짐(loud PM~다음 PM) 미포함 시 확장
        *(2026-07-22 재확정 — 멘토 7/21. 구 "사이클 종료 트리거·3개월 3사이클" 대체)*
```

**신규 결정 (2026-07-07):**
1. **Y-임계 레짐 갱신**: B9(crazy)·C2(C65 기준) 등 Y-분위수 임계는 고정이 아니라 **가한계 모드에 편입** — Phase 1 광폭/보류 → Phase 2 정식 재산정 시 최근 예측 분포로 1차 갱신(라벨 불요) → 라벨 도착 후 보정. (미갱신 시 새 레짐에서 오발동 폭주: seg2 평균 1,123 vs 고정 임계 1,572) **관리 단위 = 챔버별** (M4 — 레짐이 챔버별이므로 센서 실력치와 동일 스코프; 전역 단일 임계는 레짐 갱신 시 타 챔버 오발동 유발)
2. **대PM(요란) 후 실력치 = "신규 기준선 수립"으로 분류** — A2(0.5σ)·A3(1.0σ) 상한은 **동일 레짐 내 재설정에만 적용**, 레짐 전환 수립에는 미적용 (Qual 승인 게이트는 동일 — 헌법 1-1 정합). 미분류 시 기준선이 새 레짐에 영영 도달 불가(중심 638→1,123)
3. **가한계(provisional) 3단계**: Phase 0(개방~Qual: 알람 억제) → Phase 1(Qual~정착 ~1,000장: 중심만 이동·광폭 4~5σ·provisional 플래그·추세 룰 정상 가동) → Phase 2(정착: N=500·C42=0·과도 제외 정식 재산정). **Phase 0~1 동안 정기 자동 리캘리브레이션 비활성** — 중심선 관리는 가한계가 전담하고 Phase 2와 함께 재개 (동시 가동 시 리캘리가 과도 곡선을 0.5σ씩 추종하며 가한계와 충돌 — R2). **정착 판정 = 고정 1,000장** (A7 요란 값과 동일). Phase 2에서 일괄 수행: 정식 재산정 + 새 레짐 스냅샷·산포 확정(R5) + **CT②(AE) 정식 재학습**(과도를 정상으로 학습하는 것 방지 — R6; Phase 1의 AE 알람은 억제가 아니라 "과도기 컨텍스트" 표기로 문맥화) + Y-임계 1차 갱신
4. **A7 개정 (PM 결정, B 통보 — 2차 정정)**: Seasoning 제외 = **조용 판정 10장 / 요란 판정 1,000장** [F6] — 기준은 작업 유형이 아니라 **Qual 판정 결과** ("대PM+조용"이면 10장: 편차 없는 정상 데이터 990장을 버리지 않음)
5. **Model R2R 경계 = "요란 판정 PM"** (모든 리셋 아님): 소정비는 레짐 연속 — bias·라벨 유지. 차단된 옛 라벨도 CT① 학습에는 사용. 승인된 Recipe 튜닝(D6 명목 대비 누적 ±3% 이내)의 Y 영향은 상한(±1×RMSE) 내라 rolling bias가 자연 추종 — 리셋 불요 (M2); 전면 레시피 교체(F11의 −240급)는 R2R 범위 외
6. **모델 = 단일**: 레짐은 센서(C17류)가 실시간 운반 [F10]. regime별 분리 기각(스위칭 순환·표본 파편화·새 레짐 미해결). 시뮬 데이터로 모델 성능 주장 금지(합성 Y 순환) — 성능 평가는 실측 15,919장 + **레짐 홀드아웃 분할**로
7. **레짐 첫 bias = "신규 수립" (R9)**: 당 레짐 라벨 ≥500 축적 시(≈D+62) 대형 오프셋을 **승인 1회 경유**로 적용 — 상한(C7) 예외 (신규 결정 2와 대칭: "사람이 큰 결정을 승인, 기계가 잔잔한 유지를 자동"). 이후 rolling은 상한 내 자동. **요란 인지 상태(가한계~재학습 전)의 clamp 초과는 신규 Incident 생성 없이 기존 레짐 Incident에 귀속** + CT① 우선순위 신호로만 (알람 중복 방지). 자동 rolling 적용 최소 표본 = 50 (SE 근거) — 값은 A(팀원 A) 확인
8. **가한계 챔버의 TTTM 유예 (M1)**: Phase 0~2 완료 전 챔버는 ① TTTM 판정·자동 inhibit(B8) 대상에서 **유예**, ② **fleet median 계산 표본에서 제외** (극단값의 갭 오염 방지) — "Qual이 승인한 복귀를 TTTM이 즉시 inhibit"하는 자기모순 차단. 이 구간의 챔버 감시는 자기 Qual 스냅샷 절대 비교(B2의 병렬 축)가 대체하므로 감시 공백 없음. Phase 2 완료 시 TTTM 복귀
9. **라벨-프리 레벨 프라이어 (2026-07-08 검증 완료 — A·B 승인 대기)**: 요란 판정 시 D+0 Qual 읽기로 새 레짐 Y 레벨을 라벨 없이 추정 — §4 타임라인의 "예측 공백(D+0~60)"을 "넓은 불확실성의 추정"으로 격상. **구성 3요소**: ⓐ **주채널 C17** — Qual 5장 C17 델타 × 앵커 기울기(−6.96, **Qual 읽기 기준 캘리브레이션**; 전체평균 기준은 계통 편향 −17%라 기각). ⓑ **과도 몫 = 감쇠 템플릿**(τ=12일·초기진폭 A 이월 [F6]; 과도 실시간 추적 센서는 스캔 결과 부재 — 시계 기반). ⓒ **교차확인 C12·C11_min** — C17 읽기 고장 감시(임계 3σ_gap=75: 오경보 0.2%, 대형 고장 탐지 99%+, 고장 시 백업 추정 생존 오차 ~14). *C12는 인과 아닌 setpoint 도장이라 주채널 불가·확인용만, C63·C61 제외.* **커버리지 실측(인샘플)**: 무보정 486 → 프라이어 141 → +템플릿 **64** (초반 14일 681→79). **용도 한정**: 추정 표시·가한계 폭·bias 초기값 (D+60 라벨 도착 즉시 rolling이 인수) — 정밀 보정 아님, '추정' 딱지 필수. **전이 원장(필수)**: 요란 전이마다 (Δ채널 벡터, ΔY 점근, τ, A) 기록 → 기울기·템플릿·임계 재캘리브레이션 (n=1 앵커가 회귀로 성장 = 운영할수록 정확; 채널 가중 학습도 원장 축적 시 가능). **fleet 풀링**: 챔버 간 전이 공유로 축적 6배 가속 — 시뮬 리허설 검증 항목. 최종 일반화 검증 = 시뮬 리허설 (인샘플 n=1 한계). 스크립트: check_c17_prior · check_prior_ensemble(v1·v2) · check_transient_tracker |
10. **레짐 전환 Incident 명시 개설 (P2 — 2026-07-08)**: 요란 판정 시 그 자체를 **하나의 Incident로 명시 개설** (`type=regime_transition`, `thread_id=incident_id` — 헌법 1-4 정합). 수명: 개설(요란 판정) → 종결(Phase 2 정식 재산정 + R9 신규 수립 완료). 이 기간 같은 `chamber_id`의 후속 알람·clamp 초과(신규 결정 7)는 신규 Incident 없이 **이 Incident에 병합**(의심 윈도우 확장·Brief 갱신). 재오픈 정합: 종결 후 동일 챔버 재요란은 새 regime_transition Incident. 목적 = 요란~재학습의 여러 조치(복귀·가한계·신규수립·CT②)를 한 사건으로 묶어 승인 추적 일관화

**정직한 한계**: ① 대PM~라벨(60일) 예측 공백 — 감지·Qual·프라이어(신규 결정 9)·보수 운전이 대체 (라벨 지연 VM의 물리 한계), ② 표본 크기(사이클 2·대PM 1·조용 전/후 쌍 0·전이 1) — 분포·전이 기울기는 세계관/인샘플, ③ 말기 미관측 — 평온 가정+주입 대체, ④ 단일 챔버 — 멀티챔버·TTTM·fleet 전이 풀링은 시뮬 검증, ⑤ 소정비의 센서 서명은 미검증 가정

---

## 5. 팀 전달·미결

| 대상 | 항목 |
|---|---|
| B 팀원 B | A7 차등(10/1,000) 통보 · 가한계 3단계 제안 (+**가한계 중 정기 리캘리 비활성** — R2) · **Qual σ-갭 판정식**(기준=직전 사이클 전체·과도 제외·C12 제외·3σ — F12·R1) · Y-임계 레짐 갱신(B9, **챔버별** — M4) · "신규 기준선 수립" 분류(A2·A3 적용 범위) · **가한계 챔버 TTTM 유예·median 제외** (M1) · **정기 리캘리 실행 주기 미정** (제안: 500장마다) · 처리율 231 정정 · **A4 rolling window N=500 재검토**: 처리율 231 정정으로 500장 = 약 2일치(구 3일치)로 짧아짐 — "최근 구간" 의도 유지 위해 창 길이 재확인 |
| A 팀원 A | C12·C17 피처 잔존 확인 · C23 프록시(누수 유사) 주의 · **레짐 홀드아웃 평가 추가**(랜덤 CV는 성능 과대: 76 vs 513) · c-c 평가셋 최근 라벨 가중 · C2 Y-기준 갱신 · **CT²(AE) 정식 재학습 시점 = Phase 2** (R6) · **C11 사실관계 공유**(정착 0.13σ 침묵 / wf_min +6.56σ 반응 — "레짐 프록시" 분석과 우리 분석 모두 사실, wf_max는 −1 포화 상수 — F14) · **재분할 조건** *(2026-07-08)*: 3분할 통합 후 재분할은 **시간(레짐) 기준** — 랜덤 재섞기는 시간피처 성능 착시(본 적 있는 레짐 시험)를 유지시킴, **C23_te fold도 동일 기준**으로 재계산 · hour 시뮬 재현 결정(F15) 통보 · **⚠ 레짐 플래그 네이밍 재조정**: A의 EDA(`02_EDA_regime`)는 `is_high_regime`/`high_regime_days`(major/minor **작업 유형** 기반)로 구현됨 — 우리 결정은 **판정(조용/요란) 기반** `is_post_loud_pm`. 현재 데이터(관측 PM 1회=요란)에선 값 동일이나 **갱신 규칙이 다름**(유형이 아니라 Qual 요란 판정에서만 1). pm_log에 verdict 필드 추가로 재조정 필요 · **피처 유지 계약 (2026-07-08 축소 확정)**: 시간피처 6종 + C6 + **C17만 필수**, C11은 wf_min/정착 집계 형태 권장(F14 — gain 상위), ~~C31·C15·C16~~ 강제 철회(일상 운전 부동 → SHAP 미등장, R2R 어휘는 F16대로 C6+Process KB가 담당), 챔버는 챔버별 bias 흡수, 나머지 다이어트 자유 · ~~**C25 결정 요청**~~ **답 도착 — 기각 (2026-07-10)**: 레짐 프록시 confound 확인(gain 23위·투입 시 +3.31 악화), 시뮬 결합 불요 (§5-1 종결) · **라벨-프리 프라이어 구현(신규 결정 9)**: 요란 시 bias 초기값·'추정' 표시·전이 원장 테이블·재캘리브레이션은 A 소관 — F17 검증 완료 (C17 앵커 −6.96 Qual 기준, 커버리지 486→64, 교차확인 C12·C11_min) |
| 멘토 | 대PM 주기 3~4개월(실효 119일) 확인 + C3478 확정 (이 문서 프레임으로) + ~~Q5·Q6~~ **답 도착 (2026-07-09 → §5-1 반영)**: Q5 가변 para 확정(절반 해소)·Q6 "한 번에 해결 불가"(is_success 재정의) |
| C 팀원 C | **Process KB에 레시피 손잡이 매핑 테이블 추가** — ~~F16/0b 표(C12·C31 포함)~~ **손잡이 어휘 확정 (2026-07-11): 파워=C1·가스=C4/C5·시간=C41만. C12·C31·C17은 손잡이 금지** (C12=Vdc 도장·C31=실측 반응·C17=온도 모니터 — 데이터 사전 §상단 🎛맵이 단일 소스). SHAP 축→손잡이 변환용 · **GPU: g5.xlarge 단일 ✅확정** (2026-07-10 벤치 — qwen3:30b-a3b+bge-m3, dual/g6e 불필요) · ~~cases_synthetic_v1.jsonl~~ **cases_synthetic_v2.jsonl** 시딩 (7/11 재생성분) |
| 시나리오 | **시나리오 4(BM→정비)** 추가 [PHM 근거] — 우선순위 1~3 뒤, 최소 Scorecard 검증 · 시나리오 3은 F11(레시피 실험 −240) 실측 앵커 사용 · **주입 시점 지침 (M3)**: 주입은 정착 구간이 기본, 가한계 구간 주입 시나리오는 별도 채점 기준 (광폭 한계로 인한 감지 지연은 설계된 보수 운전) |

### 5-1. 보류 칸 (외부 확정 대기 — 도착 시 끼워넣기, 마감에 지장 없음)

| 항목 | 대기 대상 | 준비 상태 |
|---|---|---|
| ~~**C3478**~~ **멘토 확정 (2026-07-11) → C3·C4 재확정 (2026-07-22, 멘토 7/21)** | — | C7=±1×RMSE / C8=요란 후 rolling N=500·loud만 리셋 (유지). **C3·C4 갱신: pm_cycle_end·3 cycle → 일간 슬라이딩·365일 창 + 완결 요란 레짐 조항** ("실시간 스트리밍 1일" + 현업 대PM 3~4개월×최악 3사이클). config 13차·기획서 v4.6-d |
| ~~**Q5 C6_1 레시피 정의**~~ **절반 해소 (2026-07-09 멘토) + setpoint 탐사 완료 (2026-07-11)** | 레시피 정의서 원문(멘토) | **가변 para = temp·gas유량·pressure (+파워·시간), temp 1순위** 확인. temp 가변 시 EPD·장비 상태 para 연동 변화 = "12+개 동시 변화"의 정상 원인 → 손잡이 후보 압축의 현업 근거 (F16). **손잡이 컬럼 확정 (7/11 전수 탐사 — 지문 스캔+레시피 함수+상수 값 대조 3단계)**: 가스=C4·C5, 파워=**C1**(C31은 실측 반응 — knob 아님), 시간=C41 → **3축 확보**. **temp·pressure setpoint = 데이터 수집 채널 밖 확정적**(상수 6종 값 {0,1,14}로 온도 대역 무관·C57/C58은 레시피 불변으로 압력 설정 아님) → 두 축은 손잡이 미대응, **에스컬 라우팅** 처리. 부활 경로 = BL8(목표 지정형 — C17 운전점 지정, 합성 Y 모드와 묶임). **레시피 정의서 원문 = 입수 불가 확정 (2026-07-11)** → temp·pressure 축 해소는 **BL8로 완전 단일화 + 당일 채택 확정 (PM)** — W5 P5-3(합성 Y 모드)에서 구현 (Q5 항목 이것으로 최종 종결 — 구두 답변분(가변 para·temp 1순위)만으로 운용). ※ C17이 서보 대상이 아닌 부품 온도 모니터라 레짐을 정직히 반영 = 프라이어 주채널 근거 |
| ~~**Q6 FTFR 상수**~~ **답 도착 (2026-07-09 멘토)** | — | "조치 한 번에 해결은 불가·구조불량 상존·불량률 저감 방향으로 튜닝" → **is_success 의미 = '해결'이 아니라 '개선'으로 재정의** (상수 0.7/0.8/0.2 유지). 합성 사례 재생성 후 전달 |
| ~~**P1 σ-단위 상한** (A2·A3 %→σ)~~ **확정 종결 (2026-07-14, B4-4)** | B 팀원 B 확정 | **A2 = 0.5σ / A3 = 1.0σ** (σ 기준 = A2 현행 관리선 σ · A3 사이클 시작 σ 고정, max(0.5σ, 분해능) 하한). 근거: N500 표본오차(≈0.045σ)의 ~11배. 합의안 A2·A3 반영 완료 |
| ~~**C25 채택 여부**~~ **기각 확정 (2026-07-10, A 팀원 A)** | — | EDA +0.459는 레짐 통제 전 confound(레짐 프록시) — M2 gain 최고 23위(0.06%)·직접 투입 시 CV +3.31 악화. v8의 is_high_regime·high_regime_days가 신호 흡수. **PM 시뮬 C25-Y 결합 구현 불요.** 근접 후보 F-T15·F-P3는 관찰 트랙 동결(M5a~M8 병행 추적 — 채택 게이트 ΔCV ≤ −0.3 미달) |
| **레짐 플래그 재조정** (`is_high_regime`→판정 기반) | A 팀원 A (모델링 완료 후) | 전달 완료 — 값 동일, 갱신 규칙만 변경 (pm_log verdict 필드) |
| ~~**신규 결정 9·10 최종 승인**~~ **승인 완료 (2026-07-11 회의 — A·B·멘토 컨펌)** | — | 검증 완료 → **확정**. 프라이어는 ⓟ1(v8 기준 감쇠 템플릿 거취 재실측 — A, A4-3 착수 전)만 시점부 잔여 |

## 6. 인용

- PHM Society 2018 — Ion Mill Etching (고장 3형·간격·"고장시각=정지시각" 난제)
- BOSCH Plasma-Etching (Zenodo 17122442) — conditioning 실험 구조·파라미터 31종
- Wet clean 문헌 (Corial/Plasma-Therm, US6197699) — 수백 RF-h·2~3천장·2~4주
