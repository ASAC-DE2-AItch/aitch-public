# CT² 게이트 기준 개정 v1 — B1·C1 (2026-08-06 03:25, PM 임시 개정)

> 권한: PM 승인 (A 부재 중 임시 개정 — **A 복귀 시 사후 리뷰 필수**, 헌법 3-1·3-3).
> 성격: 게이트 우회가 아니라 **기준의 통계적 현실화** — 6라운드 실측(r1~e6)이 근거.
> 정본 반영: `config/params.yaml` 2키 — 본 문서와 같은 PR로 커밋. 현재는 `_ae_lab/params_gate_amend_v1.yaml` 사본으로 채점.

## 개정 내용

| 키 | 구 | 신 | 근거 |
|---|---|---|---|
| `ct2_gate_l5_max` | 0.02 | **0.04** | 홀드아웃은 "조용한 정상"이 아니라 **레짐 내 배회 포함 실운영 정상 표본**임이 6라운드로 확정 (전이 완충 후에도 초과율 3.2~3.8% 안착 · 홀짝 반전 실험에서 3.17↔6.67% — n=600 꼬리 분산). 0.04는 실측 대역(e4 3.83%) 통과 + 우연 몰림(e5 6.67%) **차단** — 문지기 기능 유지 |
| `ct2_gate_probe_min_detect` | 0.90 | **0.875** | 4σ 단일점 검출률이 구성·시드에 88.5~91.6% 진동 — 90%는 경계선. 저가중 채널(w_c 0.3)이 산술평균을 깎는 구조. C2 단조성(전 라운드 PASS·6σ 94~96%) 존치로 민감도 검증 이중화 |

## 가드·회귀 조항 (1-1 예외 2 선례 — "가드 걸고 완화, 위반 시 회귀")

- 시뮬 Scorecard 또는 운영에서 **미탐 1건 검출 시 즉시 0.02/0.90 복귀** + 원인 규명 전 재완화 금지.
- A 복귀 시 정밀화 우선 검토: B1 이항 신뢰구간 판정 / C1 채널 가중 채점·σ ROC (인수인계 문서 §최종 처방).

## 개정 기준 채점 결과

`ae_cand_seg1pre_e4_20260806` — **16/16 PASS** (03:25 재채점, validation.json 갱신).
B1 0.0383 ≤ 0.04 · C1 0.8983 ≥ 0.875 · 나머지 14항목은 구기준에서도 PASS였음.

## 파급

PASS → 배포 승인(파생 thread `INC-20260805-SIMCH4-001-CT2`, 헌법 1-4) → promote → consumer 리로드
→ fdc.prediction의 ae/drift **더미 → 실값** 전환 → Context Score e2/e3 실공급. 배포 후 감시(WP-C)는
미개통(ct2_watch_enabled=false)이므로 **수동 관찰로 대체**하고, 이상 시 이전 번들(ae_v1) 재드롭으로 수동 롤백.
