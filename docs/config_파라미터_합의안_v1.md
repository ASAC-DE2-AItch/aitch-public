# Config 파라미터 합의안 v1.8 — 회의용 *(제목 정합: 7/7 브리핑 "v1.7" 표기 + 10차 개정 반영 / 2026-07-06 개정: 멘토 확정 반영 — A1·A4·C3·C4 갱신, C7~C9·B9·D6~D8 신설 / 2차: E6 신설·C3·C4·C7·C8 보류·B9 상향 제안 / 3차: 결정 기록 정정 — **PM 권한 항목(E1~E6·B5·B6)만 확정** / 4차: **D 섹션 C(팀원 C) 검토 반영** — D1~D6 확정, D7·D8 수정, D9~D19 신설 / 5차: **A·B 섹션 B(팀원 B) 검토 반영** — 데이터 근거 확정, B9=P99(1572) · **LLM 로컬(AWS 셀프호스팅) 운용 확정 (PM)** · **E6 = 2개월 ≈ 3,000 wafer 재환산 확정 (PM)** / 6차: **D 섹션 C 검토 v2** — D8 재정의(KB 4종 합산 1만 청크), D20~D22 배포/양자화 신설, 회의 우선순위 명시 / 7차: **E6 재정정 (PM)** — 날짜 정보 기반 처리율(≈170 wafer/일) 확인, **비율 정의로 전환** (label_delay = 2개월/PM주기 × cycle ≈ 10,200 wafer) — 기존 3,000은 부분 세그먼트를 사이클로 오인한 환산 / 8차: **D 섹션 회의 결과 — D7·D8은 C(팀원 C) 위임, 나머지 D 항목 확정** · **D4-보조 확정 (cutoff 31)** · C3478은 보류 유지 (오늘 회의에서 A 모델 결론과 함께 확정) / 9차: **처리율 231 재정정 (2026-07-08)** — 구 170은 train 단독 과소집계, 3분할 합산 15,919÷69=230.7 실측 → E6 절대환산 10,200→**13,900**, A4 rolling window N=500 창 길이 재검토 B 이관. C3478 여전히 보류 / **10차: 대량 확정 (2026-07-10~11)** — C3478 멘토 확정(C3=pm_cycle_end·C4=3·C7=±1×RMSE·C8=요란후 rolling500) · **C2 σ-갭 교체** · **D7=bge-m3 hybrid·D9=qwen3:30b-a3b·D20=g5 단일** (C 벤치 확정) · **B 신규 9종 등재** · min_labels 50 · control_limits(v4.6 16테이블) / **11차: 섀도 재정의 (2026-07-22)** — A5·A6·B6 개정: 섀도 = drift 재설정 전용(Phase 2 신규 수립 제외) · 방식 = 소급 확정 (멘토 "현업 무용" 질의 후속 — B·PM 합의) / **12차: B6-3 등재 (2026-07-22)** — A7 키 2분리(조용 10/요란 1000)·A11 provisional_k=5 신설 + **y_thresholds 신규 테이블 승인** (Y-임계 P95·P99 스팟 컷 — control_limits 밴드 구조와 불일치, 신규 결정 1 구현체. PK·이력 패턴 조건부) / **13차: CT① 재학습 재확정 (2026-07-22 — 멘토 7/21 피드백)** — C3=일간 슬라이딩(구 pm_cycle_end)·C4=365일 창+완결 요란 레짐 확장 조항(구 3 cycle). 근거 = 현업 대PM 주기 3~4개월 → 최악 3사이클 1년. 생성만+게이트 불변 / **14차: D9 모델 갱신 + E7 신설 (2026-07-28)** — D9=**qwen3.6:35b-a3b** (C 재대결 v2: 11/12 vs 30b 9/12, 리포트 think 유출 없음) · **E7 신설**(M3 ② 월드스테이트 — simulator.inject_control·persistent_run) · B4-1 배점 키 등재는 공용 브랜치(context_score) 머지 시 16차 이월 / **15차: B9-a 신설 — 미확정 항목으로 기록 (2026-07-29 — C 팀원 C)** — anomaly '동반' 인정 하한. B 회신(`B4-1_계약필드_반영요청_C_v1`)으로 **눈금**(`anomaly_score` = `ae_score` ECDF 캘리브레이션 [0,1])은 확인돼 **비교 불가였던 B9 원문 `3×기준`이 대체 대상**임이 확정. 다만 **하한값은 미정**이라 `params.yaml` 미등재 — 예측 배선 완료 후 실측과 함께 조정 권한자(B) 확정 예정 / **16차: RTD 3단 키 3종 등재 (2026-08-04 — PM, PR #103)** — `incident.rtd_consecutive_k=18` (가안 — B 검증 회신 8/4: 골짜기 16~19·최단 20 = tttm_window_wafers(20) 결박 **구조적** 확정 · 감지창 = 공통 이동 19장+ 지속 시 발화, 11장 미만 구조적 미탐 · replay 캘리브레이션값 — 문서정합 후 확정) · `rtd_scan_rollups=4000` · `rtd_isolated_min_chambers=2` (경로② 미구현 — not_wired))*

> 2026-07-03 작성. 목적: 문서 곳곳에 "예:"로 남아있는 매직 넘버를 **초기값 + 조정 권한자**로 확정 (헌법 6-1: 하드코딩 금지 → `config/params.yaml` + `.env`).
> **원칙**: 지금 정하는 건 "정답"이 아니라 "출발값". 시뮬레이션 돌려보고 조정 권한자가 근거와 함께 변경 (변경 시 config PR + 사유).
> 회의에서 각 항목 ✅/수정만 표기하면 끝 — 30분 목표.

## A. 실력치 엔진 (조정 권한: B 팀원 B) *(2026-07-06 B 검토 반영 — 전 항목 확정. 태그: 🔒 멘토확정 · ✅ 수용 · 🔧 초기값(추후 튜닝) · 📊 데이터로 확정)*

> **데이터 근거 (2026-07-07 실측 정정 — `docs/사이클_정의_v1.md`가 단일 소스)**: C65 분포 P50=1012 · P90=1311 · P95≈1404 · P99≈1572 · max=1678 (train 기준). 3분할은 동일 기간의 wafer 랜덤 분할 — **전체 15,919장/69일 → 처리율 ≈ 231 wafer/일** (train 단독 170/일은 과소). PM(C33 리셋) 1회: seg1은 **~25일 완결 사이클**(C33 0→39 상당), seg2는 47일+ 진행 중(C33 1→74). 리셋 직전 상태 양호(정기·수명 기반 개방), 리셋 직후 **지속 shift +485 및 과도 1,000+장** 실측. → A7 재검토·A8·B9·E6 환산의 근거.

| # | 파라미터 | 추천 초기값 | 근거 |
|---|---------|-----------|------|
| A1 | 관리한계 폭 k (±kσ) | **3.0** 🔒 | **멘토 확인 완료** ("평균 ± 3σ") + Nelson/Shewhart 표준 |
| A2 | 1회 재설정 폭 상한 | **0.5σ** ✅ *(2026-07-14 %→σ 확정 — B4-4)* | N500 재산정 표본오차(≈0.045σ)의 ~11배 — 정상 드리프트는 통과, 비정상 점프는 차단. **σ 기준 = 변경 전 현행 관리선 σ**. **정기 리캘리브레이션의 자동 적용 한계도 겸함** (초과 시 승인 전환). 분위수 그룹 상한은 B4-3 별도 |
| A3 | 누적 재설정 상한 (PM 사이클 내) | **1.0σ** ✅ *(2026-07-14 %→σ 확정 — B4-4)* | A2×2 — 기존 5:10 비율 유지. **σ 기준 = 사이클 시작 σ 고정**. 연쇄 재설정으로 기준선이 기어가는 것 방지 |
| A4 | 재산정 산식 + rolling window N | **rolling mean ± 3σ** 🔒 / **N = 500 wafer** 🔧 | 산식은 멘토 확정 (EWMA 폐기). N=500 ≈ **약 2일치(처리율 231/일) = "최근 구간" 의미** ('3개월 환산' 아님 — B 정정, PM 처리율 반영). E2 확정 후 재점검 |
| A5 | 섀도 평가(소급 검증) 표본 | **30 wafer** ✅ *(2026-07-22 개정 — B 합의)* | 통계 최소 표본. **방식 = 소급 확정**: 직전 이력 30장에 후보 관리선을 재적용해 즉시 채점 — 앞으로 N장을 기다리는 방식 아님(지연 0, 구 문구 "데모 배속 시 수 분"은 대기 방식 잔재라 삭제). **적용 대상 = drift 이상 대응 재설정 · 상한 초과 승인 전환만.** ⓐ 상한 내 정기 자동 재산정은 섀도 없이 즉시 적용 (B) ⓑ **Phase 2 신규 수립(요란 PM)은 섀도 제외** — 비교 프레임 부재: 구 한계 대비 = 자명한 만점 / 가한계 대비 = 조이는 방향이라 "감소" 불성립 / 자기 표본 대비 = 구성상 만점. Phase 2 검증은 Qual 4지표 + 정착 1,000장 게이트 + R9 승인 + reopen(B6)이 담당 (멘토 "현업 무용" 질의 후속 정리) |
| A6 | 섀도 통과 기준 | **미탐 0건 & 가성알람 ≥ 35% 감소** ✅ | M5 지표와 동일 — 미탐 0건이 안전 하한. *(감소율 비교 대상 = 현행(구) 관리선 — drift 재설정 전용 지표, A5 개정과 정합 2026-07-22)* |
| A7 | Seasoning 제외 | ✅ **개정 v2 (2026-07-07 PM 결정, B 통보)** — **Qual 판정 기준 차등: 조용 10 wafer / 요란 1,000 wafer** (작업 유형 아님 — "대PM+조용"이면 10장). **키 2분리 등재 (2026-07-22 — B6-3): `seasoning_exclude_wafers: 10` (조용) / `seasoning_exclude_wafers_loud: 1000` (요란)** | 실측 근거: 요란 개방 직후 과도가 1,000+ wafer 지수 감쇠 (1,418→998 — `사이클_정의_v1.md` F6·원칙 "대응은 판정에 건다"). 시간 기준(24h)은 배속과 충돌 — wafer 수 기준 유지 |
| A8 | 실력치 초기값 산출 표본 | **리셋 이후 정착 세그먼트(8,247 wafer) + C42=0(정착) 구간만** 📊 | 과도 구간 제외 — B3-1(실력치 초기값 테이블)에서 산출 |
| A9 | 정기 리캘리 주기 | **`recalc_interval_wafers: 500`** ✅ *(2026-07-16 등재 — B4-3, 출처 B)* | 텀블링 발동 주기 — A4 rolling window(N=500)와 **별개 키** (window=산정 표본 폭, interval=재산정 발동 간격). params.yaml limit_engine 절은 B 직접 반영 |
| A10 | 리캘리 dead band | **`recalc_deadband_k_se: 2`** ✅ *(2026-07-16 등재 — B4-3, 출처 B)* | 변화없음 판정 폭 = **2×SE**(재산정 표본오차) — 이동량이 dead band 이내면 재설정 생략 (무의미한 이력·limit_version 증식 방지) |
| A10b | 분해능 dead-band | **`resolution_deadband_k: 3.0`** 🔧 *(2026-08-06 등재 — 오탐 뿌리규명, 출처 B·PM 승인)* | 관리선 최소 반폭 = **k×계측분해능**(gage R&R 원리 — resolution을 최소 σ로 보고 k_sigma=3과 동일 계수). 이산 센서 밴드가 분해능보다 좁아 노이즈만 잡던 오탐(C57-s4류) 방어 — σ·분위수 밴드 둘 다 floor. **라이브 오탐 45.9%→12.8% 중 "분해능-밴드 불일치" 항.** 0=비활성 |
| A11 | 가한계 Phase 1 광폭 배수 | **`provisional_k: 5`** 🔧 *(2026-07-22 등재 — B6-3, 출처 B·PM 승인)* | Phase 1 임시 관리선 = center ± k·σ — 사이클_정의 신규 결정 3 "광폭 4~5σ"의 상단에서 시작. **잠정값 — W7 Scorecard 실측 후 값만 교체** (C8 선례, 비블로킹) |

## B. SPC / TTTM / Incident (조정 권한: B, Incident는 PM)

| # | 파라미터 | 추천 초기값 | 근거 |
|---|---------|-----------|------|
| B1 | TTTM Warning / Critical | **2.0 / 3.0** | 기획서 기존값 유지 |
| B2 | TTTM 참조 | **동일 공정 챔버 분포 비교(Box Plot, fleet median 정량화)** + 자기 Qual 스냅샷 절대 비교 (지정 golden 챔버 없음) | **멘토 확인 완료** — 현업 방식 |
| B3 | 역방향 룰 발동 | **동시 이탈 챔버 ≥ 3/4 (과반)** 🔧 | "N-1대 이탈 → 참조 의심"의 실용 근사. 오탐 많으면 4 상향, 미탐이면 2 하향 (멀티챔버 시나리오로 튜닝 — B). *(2026-07-21 4챔버 전환 반영 — 구 4/6·5/6·3/6. 4/4는 만장일치라 과반 재정의, 출처: B)* |
| B4 | Drift Score threshold | **0.7 (초기)** → ROC 기반 재산출 🔧 | R2 완화 전략 (기획서). ⚠️ **AE anomaly score(C6, A 권한)와 별개 신호** (Drift = 장기 분포 이동용) — 신호 주체·용도 A와 정렬 필요 |
| B5 | Incident 병합 시간창 | **같은 chamber, 최근 20 wafer 내** | 시간(30분) 기준은 배속과 충돌 — wafer 수 기준 |
| B6 | Incident reopen 판정(verifying) | **조치 후 30 wafer 내 동일 센서·룰 재발** | 표본 30 = A5와 정렬. **적용 후 사후 검증의 주채널** — 섀도는 승인 전 소급 채점이므로 "적용 후 30장 감시" 역할은 reopen이 담당 *(2026-07-22 정리)* |
| B7 | Context Score → Agent 가동 | **≥ 31** (미만은 기록만) | v4.2 계약 유지 — LLM 비용 절감. **G8 확정에서도 불변**(B7-a 참조 — 게이트를 안 건드리는 조합을 고른 이유) |
| B7-a | Context Score 배점 (e1·e3·스트림 결합) | ✅ **확정 (2026-08-11 — G8 선택 A, 출처: B)** | `context_score_rule_base` **{N1: 20, N2~N8: 8}** · `context_score_size_per_sigma` **2** · `context_score_stream_damping` **0.2** (신규 키 — `s₁ + Σ(나머지)×d`, 0.0=현행 max 동치). 그동안 `[가안 — Step6 Scorecard 확정]` 이던 값의 확정분이다. **근거** = `analysis/step6/results/G8_배점임계_후보_v1.md` (5차원 전수 탐색 · **AE 배점 0 전제** — B7-b). **게이트(B7 31)를 안 건드리는 조합 중 최선**을 고른 것이 실무상 결정적이다 — 게이트를 바꾸면 C fixture·계약까지 번진다. **시나리오 실측**(주입 wafer 게이트 통과율 / 첫 탐지, 구→신): SC3 다챔버공통 6%→**61%** +8→**+2장** · SC4 스톰 16%→**93%** +4→**+2장** · SC5 진동 0%→12% +511→**+2장** · SC2 스파이크 첫탐지 +91→**+16장**. 비용은 헛호출 1.4→**3.0건/일** = **재설계 신호선(100건/일)의 3.0%**, G8 자체 예산 30건/일 이내 *(2026-08-11 정정 — 초판의 "신호선 30건/일"은 오기. 신호선은 100건/일이고 30 은 G8 이 스스로 건 예산이다. 3/30=1/10 로 산술이 맞아 눈에 안 띄었다. 확정값 자체에는 영향 없음)*. **핵심은 damping** — max 집계는 "여러 센서가 동시에 걸렸다"는 수렴 증거를 버린다(T14: 다센서 사건 발화 스트림 평균 7.96개인데 1개만 반영). params.yaml·코드는 B 직접 반영 |
| B7-b | Context Score AE 축 배점 (e6·e7) | 🔒 **격리 — 0 (2026-08-11, 출처: B)** | `context_score_ae_band_pts` [10,20,30]→**[0,0,0]** · `ae_pts_drift_lo/hi` 10·20→**0·0**. 라이브 `anomaly_score` 가 **98.2%가 정확히 1.0**(서빙 번들 학습 구간 ≠ 시뮬 발행 구간 — 시뮬은 `replay_post_reset_only: true`)이라, 배점을 주면 **모든 알람에 최상단 밴드 30점이 상시 가산**돼 게이트가 사실상 사라진다(실측: 주입 챔버 74.6% vs 비주입 72.6% — **구별 0** · 헛호출 167.8건/일). e7 은 `clip((EWMA(ae)−B0)/(0.2−B0),0,1)` 이라 **e6 포화의 종속**(실측 98.7%)이므로 같이 끈다. Qual `use_ae_axis: false`(PM 8/6)·RTD `exclude_ae_arm: true`·B9 `use_anomaly_arm: false`(PM 8/11)와 **같은 논리**이며, **AE 포화 해소 확인 시 되돌린다**(e4·e5 격리 선례의 회귀 조항) |
| B8 | 자동 inhibit 조건 | **severity = Critical (N1, TTTM > 3.0)** | 라우팅 매트릭스와 일치 |
| B9 | crazy wafer 판정 | **predicted_c65 > 1572 (train P99) 또는 anomaly_score > 3×기준** (1장 spot) 📊 | **B 확정 — 데이터 기반 P99=1572로 시작.** PM의 P99.9 상향 제안은 시뮬에서 오발동(무승인 자동 HOLD) 실측 후 재검토. OR 유지 — 미탐(스크랩 누락, rework 불가) 비용 > 오탐(HOLD 후 RELEASE). 자동 HOLD → 승인 스크랩. 연속 2장 이상 = Incident 경로 (v4.5). 참고: Qual C2의 "C65 ≤ P95"는 P95=1404 (같은 분포) |
| B9-b | B9 anomaly arm 스위치 | 🔒 **격리 — false (2026-08-11 PM 승인, 출처: B)** | `spc.crazy_wafer.use_anomaly_arm` **신규** — 코드 기본값 `true`(구 config 는 현행 동작 유지), params 에 명시해야만 격리된다. **predicted arm(`> P99`)은 불변.** 사유: 컷 = `anomaly_score_multiplier(3.0) × ct.ae_anomaly_threshold(0.2) = 0.6` 인데 라이브 anomaly 가 98.2%가 정확히 1.0 이라 상시 초과 → **wafer 40~57%가 crazy 판정**(실측). 그 알람은 대표 위반이 없어 하류에서 `tuning=None` → `insufficient_evidence` 로 종결 — 신호는 없고 기록만 쌓인다. ⚠️ **컷 조정은 대안이 아니다** — 포화가 정확히 1.0 이라 1.0 미만 컷은 전부 발동하고 1.0 초과는 영구 미발동(=잠금과 동일). 그래서 값이 아니라 **스위치**다. AE 포화 해소 시 `true` 로 되돌린다 |
| B9-a | anomaly '동반' 인정 하한 | ⬜ **미확정 — 값 미등재** *(2026-07-29 기록, C 팀원 C)* | B9 원문 `3×기준`은 **비교축으로 쓸 수 없다** — 그 "기준"이 문서에 정의된 적이 없고, A 규격이 `ae_score [0,1]`이라 `3.0` 직접 비교가 성립하지 않는다(라이브 전이라 미발동 상태로 잠복). B 회신(`B4-1_계약필드_반영요청_C_v1`, 2026-07-28)으로 **눈금은 확인**됐다 — `anomaly_score` = `ae_score`의 ECDF 캘리브레이션 [0,1] 이라 config 임계와 **직접 비교는 성립**한다. 다만 **하한을 얼마로 둘지는 별개**다 (`qual.pass_ae_max`=0.2 는 *챔버·가역* 판정이고 SCRAP 동반 하한은 *wafer·불가역*이라 같은 값이어야 할 이유가 자동으로 성립하지 않는다). **예측 배선(wafer별 수치 조회) 완료 후 실측과 함께 조정 권한자(B)와 확정** → 그때 `spc.crazy_wafer.anomaly_score_min` 등재. ⚠️ 그때까지 **판정은 보류**된다 — 코드는 값 유무가 아니라 임계 비교로 SCRAP 을 내므로(유무 판정이면 `0.01`(정상)도 동반 충족이 돼 과잉 발동), 키가 없으면 SCRAP 미발행(fail-safe) |

## C. Qual / CT (조정 권한: A 팀원 A)

| # | 파라미터 | 추천 초기값 | 근거 |
|---|---------|-----------|------|
| C1 | Qual wafer 수 | **5장** | 팀 리뷰 값 유지 |
| C2 | Qual 기준 4종 | **AE < 0.2 / ~~TTTM 갭 < 1%~~ → σ-갭 < 3σ (2026-07-11 회의 확정 — 기준 산포=직전 사이클 전체·C12 제외·과도 제외, F12) / Nelson 0건 / C65 ≤ train P95 (proxy 예측값 — 과도기 기여 없음 명시)** | 사이클_정의 Qual 실행 스펙과 정합화 |
| C3 | CT① 트리거 | ✅ **일간 슬라이딩 재학습** *(2026-07-22 갱신 — 멘토 7/21 피드백. 구 `pm_cycle_end` 7/11 대체)* | 멘토: "실시간 스트리밍이니 1일이 좋다". 매일 재학습(생성만)하고 가장 오래된 하루를 버림. 배포는 champion–challenger 통과 시만 (헌법 3-3 — 불변). Cycle 내 격차는 Model R2R(bias)로 대응(불변). **A 구현 조율 대기 (트리거 캐던스)** |
| C4 | CT① 학습 창 | ✅ **`retrain_window_days: 365` (1년)** + **완결 요란 레짐 확장 조항** *(2026-07-22 확정 — PM. 구 "3 cycle" 대체)* | 근거: 현업 대PM 주기 3~4개월 → **최악 3사이클(완결 2 + 진행 중) = 12개월**. 구 240은 진행 중 사이클 누락 과소치. **창 내 완결 요란 레짐(loud PM~다음 PM) 1개 미만이면 직전 완결 요란 레짐까지 임시 확장, 충족 시 365 복귀** (희소 전환 표본 보장 — 요란은 대PM의 30%라 1년 창도 34% 확률로 미포함). 워밍업 = 창 찰 때까지 누적. 시뮬 리허설서 240/300/365 스윕 여지 (조정 권한 A) |
| C5 | CT① 승격 기준 (champion–challenger) | **valid RMSE 3% 이상 개선 시에만 교체** | 동률 교체로 인한 불안정 방지 |
| C6 | AE anomaly threshold (운전 중) | **0.2 (Qual 기준과 동일값에서 출발)** | 일관성 우선, ROC로 재조정 |
| C7 | Model R2R bias 상한 | ✅ **±1.0 × train RMSE** (clamp, 초과 = Incident 신호 — 선진행 7/10) | 멘토 확정 시 값만 교체 |
| C8 | Model R2R 산정·발동 | ✅ **"마지막 요란 PM 이후" 라벨 잔차 rolling mean (N=500)** — 소정비는 bias·라벨 연속, **요란 판정 PM에서만 bias=0 리셋 + 이전 라벨 차단** (`reset_on: loud_pm_verdict`, 선진행 7/10). 이전 레짐 잔차 전이 불채택 | 데이터 검증 (무보정 513 / 전이 605 / 당레짐 85) + 레짐 경계 = 요란 PM (`사이클_정의_v1.md` §4) |
| C9 | 평가지표 | **RMSE(주) + R²(보조 — 현업 중시)** | ⚠️ 현 R² 비정상 고평가 → 누수 검증 선행 (A 최우선) |

> ~~⏸ C3·C4·C7·C8 보류~~ **선진행 확정 (2026-07-10 PM)**: ~~C3=pm_cycle_end / C4=3 cycle~~ **→ C3·C4 재확정 (2026-07-22, 멘토 7/21 피드백): C3=일간 슬라이딩 / C4=365일 창 + 완결 요란 레짐 조항** (위 표 참조) / C7=±1.0×RMSE clamp / C8=요란 후 rolling N=500·`reset_on: loud_pm_verdict`(키 교체 — 全PM 리셋은 신규 결정 5와 모순) + `min_labels_for_auto: 50`(값은 A 확인 잔여) — params.yaml 기입 완료. 멘토 확정 도착 시 값만 교체 (비블로킹).
>
> **B 신규 파라미터 9종 등재 (2026-07-10 PM 승인, 출처: B 목록)**: trim_quantile 0.01 / min_trim_n 100 / min_group_n 30 / false_alarm_warn_pct 2.0(2파일 공용 — 단일 등재) / cv_warn 0.5 / activity_min 0.05 / mag_min 0.10 / time_gap_threshold_hours 72 / **context_score_pred_weight null**(B7 후속 — B4-1 착수 시 B 확정, 단독 발동 불가·가한계 Phase 0~1 가중 0) — params.yaml `spc:` 섹션. **결정 A(분위수) q_low/q_high 등재 완료 (2026-07-12 B): 0.00135 / 0.99865 (±3σ 등가).**
>
> **Qual AE 축 잠정 제외 (2026-08-06, PM 회신 — B 반영, PR #115)**: `qual.use_ae_axis: false`. **C2의 AE < 0.2 축을 `proposed_verdict` 트리거에서 잠정 제외**(σ-갭만 트리거, AE는 `ae_anomaly_mean`에 기록만). **사유**: AE 실서빙이 PM 직후 100% 포화(`anomaly_score`=1.0 > 0.2)라 Qual 5장(PM 직후 발행)이 **전건 FAIL 수렴** — AE 학습셋이 PM 이전이라 새 레짐을 이상으로 보는 정탐이나, **AE는 "레짐이 바뀌었다"를 말하고 Qual은 "새 레짐이 건전한가"를 묻는 절차라 질문이 다름**(같은 사유로 PM이 RTD 자동정지에서도 AE arm 제외). **AE 재학습(post-PM) 후 `true` 복원.** ⚠️ C2(A 조정권한)의 AE 축에 관여 → **A·PM 확인**. params.yaml `qual:` 섹션.

### C-2. lean-85 C65 예측 모델 파라미터 (2026-07-20, PR① — handoff v1.0 이관)

> `config/params.yaml[lean85]` 신설. 값은 handoff 동결 스펙과 동일 — Phase 0 로컬 venv 재현 pooled **100.080 PASS**(기준 99.84±0.5). `lean85_pipeline._load_lean85_config()` 폴백 기본값과 일치.

| 키 | 값 | 근거 |
|---|---|---|
| onset_days | 7.0 | 요란 PM 후 레짐-온셋 → `low_confidence=1` (B2′) |
| retrain_days | **1** ✅ *(2026-07-23 정합 — PM)* | 정기 재학습 주기(일). **CT① 정본 = 일간 슬라이딩 (C3 13차, 멘토 7/21 — 구 pm_cycle_end·7일 대체)** — params `lean85.retrain_days: 1`과 일치. 구 값 7은 핸드오프 동결 스펙의 잔재. PSI는 재학습 트리거 아님 — 서빙 보호·에스컬레이션 (CLAUDE.md 3-3, D안) |
| bench_pooled_rmse | 99.840 | B2′ 시간축 수용검사 기준(정본 인용). lot-CV 66.8~67.0 인용 금지 |
| bench_tol | 0.5 | 수용 허용 오차 (R6 — 공식 판정 = 로컬 venv) |
| psi_warn / psi_alert | 0.10 / 0.25 | 입력 PSI 주의 / 경보 (drift_report level) |

## D. Agent / LLM (조정 권한: C 팀원 C) *(2026-07-06 C 검토 반영 — 상태 범례: ✅ 확정 · 🔄 수정/미확정 · 🆕 신규(미확정) · ⏳ 검토 중)*

> **LLM 운용 방침 (기획 수정 — PM 확정, 2026-07-06)**: 외부 LLM API 미사용 — **로컬(오픈소스) LLM을 AWS GPU 인스턴스에 셀프호스팅** (vLLM 서빙 = D13과 정합). D9 후보 3종(Qwen3-30B-A3B Q4 / EXAONE 32B / Gemma 27B)은 이 전제에서 선정. ⚠️ 기획서에도 반영 필요 (헌법 4-3).

| # | 파라미터 | 초기값 | 상태 | 근거 / 결정 포인트 |
|---|---------|-------|------|------|
| D1 | RAG Top-K | 🔄 **3 현행화 (2026-07-30, C)** — 구 **5** (필터 후) | ✅ | 구 값의 근거는 *"Evidence Card 3장 내외 목표"* 로 **5를 검색해 3장을 선별**하는 구조였다. 그 선별이 D2 `similarity_min` 필터인데 **코드 미사용**이다 — D7 에서 임베딩을 bge-m3 **hybrid** 로 확정하며 융합이 RRF(rank fusion)가 되어 융합 점수가 cosine 이 아니고, RRF 최댓값이 `2/(k+1)` 이라 **어떤 k 에서도 0.7 을 못 넘는다**(걸면 근거가 전멸). 전제가 사라져 "5 검색 → 3 선별"이 성립하지 않으므로 실동작(3)에 맞춰 현행화했다. 3 인 실질 이유는 **프롬프트 예산** — 가변분(cases·kb_hits·logs) 한도 ≈3,600 토큰이고 이 키가 손잡이다. recipe 는 여유가 거의 0이라 늘리면 400 이 재발한다(사고 3건). Brief 근거도 계약 §6 상 3층 고정. **필터 복원 + top_k 5 실험은 D18 재검토 항목**(예산 실측 동반) |
| D2 | similarity 최소 컷 | **0.7** | ✅ | M6 지표와 정렬 (D18 확정 후 "코사인 0.7"로 명시) |
| D3 | JSON 파싱 재시도 | **2회** | ✅ | 헌법 6-3 고정 — D13 guided decoding 병용 시 최후 안전망 |
| D4 | slow-path 목표 지연 | **< 30초** | ✅ | 비동기 — 시연 체감 기준. fast/slow **분기 기준 = D4-보조 cutoff 31 ✅확정 (2026-07-07, 분기 비활성)** |
| D5 | 유사 사례 0건 시 | Evidence 없이 "사례 없음" 명시 리포트 | ✅ | 환각 방지 — 없는 근거 생성 금지 |
| D6 | Recipe Tuning delta 상한 | **setpoint의 ±3% — 명목값 대비 누적** *(구 "/ 1회" 정정 2026-08-03)* | ✅ | 승인돼도 소폭만 — 초과 제안은 에스컬레이션 (v4.5). **B5-4 엔진이 `nominal_setpoints` 대비 누적으로 산출**하므로 −2% 적용 후 다시 −2% 제안이면 delta_pct 는 −4% 다. 소비 코드(C `enforce_number_guard`)는 받은 값을 그대로 3.0 과 비교하므로 **동작 무변경 · 문구만 정정**(B 합의분 ⑤). ⚠️ 온도(`temp_target`)는 `nominal_setpoints` 에 없어 매번 현재값 대비로 재산출돼 항상 ±1% → **이 상한이 사실상 무력**(미결 D12 — B·PM 결정 대기) |
| D7 | 임베딩 모델 | **기본 BAAI/bge-m3 (dense+sparse 하이브리드)** · 임베딩 전용 대안 = **Qwen3-Embedding** (정확 표기) | 🔄 | "Qwen 계열"은 생성 LLM(Qwen-Instruct)과 혼동 — 임베딩/생성(D9) 분리. 한국어 후보 **KORPatent-BGE**(bge-m3 기반, 특허 특화)는 KB 청크 hit@k 실측 비교 후 채택 결정 (특허 도메인 ↔ 공정 로그 어긋날 수 있음). 기한: cases_v1 시딩 착수 전 (Qdrant 재생성 방지, 헌법 4-2). PDF/PPT 페이지 청킹 + Vision LLM 유지. ⚠️ 기획서 v4.5 "Qwen 계열(bge-m3 대체)" 문구와 상이 — 문서 동기화(헌법 4-3) 필요 |
| D8 | KB 시딩 목표 | **KB 전체 합산 ≈ 10,000 청크** — Historical Case 단독 아님. *(2026-07-07 KB 5타입 체계 채택 — 집계는 벡터 3컬렉션 기준인지 이력 포함인지 C가 확정)* | 🔄 | 멘토의 "1만" = KB 전체 청크 수 해석 (C v2). 결정 포인트: 타입 간 **배분 비율 합의**. near-duplicate 대량 생성 금지(D2·D5 환각 가드 무력화 방지), provenance 라벨(pm_anchored / domain_case / simulator) 유지 |
| D23 🆕 | wafer 예측 조회 챔버 스캔 상한 | **500 행** | 🆕 | `suspect_window.member_wafers`(B 발행) 부재 시 `wafer_predictions` 를 챔버 최근 N행만 훑는 상한 (`db.fetch_predictions` — wafer별 처분 판정 입력). 눈금은 A4 `rolling_window_n`(500)에서 따왔으나 **별개 키**다: A4 는 재산정 표본 폭, 이쪽은 소급 조회 범위이며 A4 주석에 *"E2 확정 후 재점검"* 이 걸려 있어 재사용하면 B 의 표본 폭 튜닝이 C 의 조회 범위를 조용히 바꾼다 (같은 값·다른 목적을 분리한 A9 `recalc_interval_wafers` 선례). 무한 스캔 시 오래된 레짐 수치가 판정에 섞인다. *(코드 상수 → params 이관: PM 권고 2026-07-30 · PR #65)* |

### D24~D26 — 코드 상수 → config 이관 *(🆕 2026-07-30 · 헌법 6-1·6-4 정합 스윕. PM 권고(PR #65 D23)의 연장)*

> 스캔 방식: `src/agent_service/app` 전 파일 AST 로 숫자 리터럴 전수(24건) 추출 → 튜닝 대상만 이관.
> **이관하지 않은 것**은 코드에 사유를 한 줄씩 남겼다 — 계약 고정값(근거 3층·evidence 2~4장·score 0~100),
> 단위·포맷(%→비율 100.0 · round 자리수 · 반올림 +0.5), 경로 깊이, 스텁 도장(`_STUB_*`, B 실물 오면 소멸).

| # | 파라미터 | 초기값 | 상태 | 근거 / 결정 포인트 |
|---|---------|-------|------|------|
| D24 | Postgres 연결 타임아웃 | **3초** | 🆕 | `db.CONNECT_TIMEOUT_SEC`. **무중단의 핵심** — 미기동 시 OS 기본(수십초~분)까지 파이프라인이 멈춘다. "빈 결과로 진행"이 의미를 가지려면 빨리 포기해야 하고, D4(30초) 예산 안에서 근거 조회가 전체를 잡아먹지 않아야 한다. DSN 자체는 인프라 값이라 `.env`, 이 값은 **예산**이라 params |
| D25 | Kafka poll 대기 | **1,000ms** | 🆕 | `source.iter_kafka_alerts`. 짧으면 CPU 회전, 길면 graceful shutdown(SIGTERM) 반응이 그만큼 늦다 — 헌법 6-2 트레이드. 기본값을 없애고 호출측(run.py)이 주입 |
| D26 | 프롬프트 예산 문자/토큰 비 | **2.2** | 🆕 | `llm.client._CHARS_PER_TOKEN`. 런타임에 토크나이저를 올릴 수 없어 문자수로 근사한다. 2026-07-28 실측 역산의 **하한**(안전측): limit 2.27·maint 2.49·recipe 2.23. 재측정하면 바뀌는 값. ⚠️ 경고용 어림이고 정밀 게이트는 `scripts/measure_prompt_budget.py`(토크나이저 실측·exit 1). **`max_model_len` 은 서버 설정이라 params 아님 — `.env AGENT_LLM_MAX_MODEL_LEN`**(구 코드의 `8192` 폴백은 2026-07-29 사고 원인이라 제거, 미설정 시 검사 skip + 경고) |

### D9~D14 — 생성 LLM 모델·추론/서빙 *(D9 ✅확정 qwen3:30b-a3b 2026-07-10 / D10·D12·D13 확정 / D11·D14 출발값)*

| # | 파라미터 | 추천 초기값 | 근거 / 결정 포인트 |
|---|---------|-----------|------|
| D9 | 생성 LLM 모델 | ✅ **qwen3.6:35b-a3b 갱신 확정 (C 재대결 v2 2026-07-28 — PM 승인 14차)** | 판정 골든셋 12문항: **root_cause 11/12·action 12/12·지연 1.2~1.3s·JSON 1.00** — 구 확정 qwen3:30b-a3b(9/12)를 동일 골든셋에서 +2문항 상회 (judge_eq_rfmatch·judge_bl_gasflow). 3.5:35b(9/12)도 탈락. **리포트 생성에서 30b는 영어 사고과정(<think>) 유출 — 3.6은 무유출 한국어 완결** (구 /no_think 완화책 불요). 전 모델 공통 오답 judge_esc_multi 1건은 골든셋 난제(action은 전원 정답 — 에스컬레이션). 가중치 = s3://aitch-agent-service/llm_cache/ (AWQ). 근거: results/llm_qwen36_vs_30_report.md·llm_qwen_3way_report.md |
| D10 | LLM temperature | **0.1~0.2** | 사실 기반 리포트 — 낮은 온도로 JSON 안정·환각 억제. 최종값은 벤치로 튜닝 |
| D11 | max output tokens | **1,500~2,000** | Evidence 3장 근거 리포트 상한. 리포트 템플릿 길이 확정 후 최종화 |
| D12 | seed 고정 | **42 (고정)** | 시연 재현성 |
| D13 | 서빙 / 디코딩 | **vLLM + guided decoding (문법 강제 JSON)** | 생성 시점 스키마 강제 → D3·헌법 6-3을 토큰 레벨 보장. 깨진 JSON 원천 차단, D3 재시도는 최후 안전망 |
| D14 | LLM 호출 타임아웃/재시도 | **20s / 1회** | 호출·네트워크 레벨 (D3 파싱 재시도와 별개). slow-path 예산(D4)과 정렬 필요 |

### D15~D19 — 검색(Retrieval) *(D17 dense:sparse=0.7:0.3은 D7=bge-m3 hybrid 확정으로 활성 / D15 chunk 400·D19는 KB 시딩(13,481) 시 사용값 / D16 reranker 도입 여부만 C 잔여)*

| # | 파라미터 | 추천 초기값 | 근거 / 결정 포인트 |
|---|---------|-----------|------|
| D15 | 청크 크기 / 오버랩 | **300~500토큰 / 15%** | 검색 정확도 직접 좌우 — KB 문서 특성 보고 확정 (PDF·PPT는 페이지 단위 = D7) |
| D16 | Reranker | **bge-reranker-v2, rerank 후 Top-K=5** | dense 한계 보완·정밀도↑ — 도입 여부 + 지연 예산(D4) 영향 검토 |
| D17 | 하이브리드 가중치 (dense:sparse) | **0.7 : 0.3** | bge-m3 하이브리드로 D2 컷 계산 시 필요 — 임베딩 확정(D7) 후 |
| D18 | 거리 메트릭 / normalize | **cosine / normalize=True** | D2의 "0.7"이 코사인 기준임을 명시 |
| D19 | Qdrant HNSW (ef_search, M) | **64, 16** | 검색 품질·속도 튜닝점 — 시딩 규모(D8) 확정 후 조정 |

### D20~D22 — 배포 아키텍처 / 양자화 *(🆕 2026-07-06 C 제안 v2)*

> **아키텍처 원칙**: 시연은 **단일 EC2 인스턴스 + Docker Compose** 전부 적재 — GPU: LLM·임베딩·reranker / CPU: Qdrant·Agent 앱(LangGraph)·시뮬레이터·대시보드. (LLM 로컬·AWS 셀프호스팅 방침과 정합)

| # | 파라미터 | 추천 초기값 | 근거 / 결정 포인트 |
|---|---------|-----------|------|
| D20 | 배포 인스턴스 | ✅ **g5.xlarge (A10G 24GB) 단일 확정 (2026-07-10)** | qwen3:30b-a3b(MoE 활성 3B) 단일 + bge-m3 임베딩으로 g5 적재 성립 — **dual 모델·g6e 불필요 확정** (순수지연 1.8s라 지연 여유). 비용↓ 확정. ~~예산 vs 안정성 미확정~~ 해소 |
| D21 | LLM 양자화 / 서빙 | 🔄 **벤치는 Ollama(GGUF Q4_K_M)로 수행** — **운영 서빙(vLLM+guided_json, D13) 전환 시 양자화·guided decoding 재확인 잔여** | A10G는 FP8 불가 → AWQ 4bit 또는 GGUF. ⚠ **미결: qwen3:30b-a3b + vLLM guided decoding 동작 확인** (벤치는 Ollama였음 — C4-3 W4에서 검증) |
| D22 | 비양자화 대상 | **임베딩(bge-m3)·reranker = FP16 유지** | 임베딩은 FP16 품질 유리 (D7 확정 = bge-m3 hybrid) |

> **D4-보조 — fast/slow-path 분기 기준 ✅ 운영 방침 확정 (2026-07-07 PM)**: 출발값 **X = 31 (분기 비활성)** — Agent 가동 건(B7 이상)은 전부 slow-path. 데모 규모에선 분기 불요 판단. **W6 alarm storm 시나리오에서 slow-path 지연 > D4(30초) 관측 시**, Context Score 분포 히스토그램으로 LLM 처리 용량을 넘지 않는 컷을 역산해 B·C가 재확정. `params.yaml: agent.fast_slow_cutoff`.

> **회의 우선순위 (C 제안)**: 시연 품질 직결 — **이번 회의 필수 확정**: D9(생성 모델) · D13(vLLM+guided decoding) · D15(청킹) · D16(reranker) · D20·D21(인스턴스·양자화) + D7·D8 수정안 승인. **출발값 후 시뮬 조정**: D10~D12 · D14 · D17~D19 · D22 · D4-보조.

## E. 시뮬레이터 / 운영 (조정 권한: PM 윤준호)

| # | 파라미터 | 추천 초기값 | 근거 |
|---|---------|-----------|------|
| E1 | 가상 챔버 수 | **6** | 기획서 유지, fleet median 성립 최소치 이상 |
| E2 | 데모 배속 | **30× (3초 → 0.1초)** | wafer 1장 ≈ 1초, 시나리오 1개 ≈ 2~3분 |
| E3 | 챔버 오프셋 폭 | **`chamber_offset_sigma: 0.1`** — 실데이터 센서 std의 ±0.1σ 내 랜덤 *(2026-08-06 PM 확정: 0.5→0.1 — TTTM 크로스챔버 과잉 완화, control_limits·Qual 스냅샷 동반 재시딩)* | 챔버 간 벌어짐 축소 |
| E4 | Pending 타임아웃 | **24h (실시간) / 데모 모드 비활성** | 시연 중 타임아웃 방지 |
| E5 | 대시보드 갱신 | **푸시(알림) + 폴링 5초(차트)** | 화면 정의서 확정값 |
| E6 | 실측 C65 도착 지연 (label_delay) *(신설 2026-07-06, 7차 재정정)* | **비율 정의: label_delay = PM_cycle_wafers × (2개월 / PM주기)** — **대PM(레짐) 사이클 기준** — 실효 사이클 ~119일(`사이클_정의_v1.md` §3)이면 라벨은 사이클의 **~0.5 지점부터** 도착 (최소 사이클 101일 > 60일 → 전 사이클 R2R 가동 보장). 절대 환산 ≈ **13,900 wafer** (231 wafer/일 × 60일, 2026-07-07 정정) | fail bit(C65)는 fab out 후 확보 — 2개월 가정. ⚠️ 기존 "3,000"은 부분 세그먼트(5,970)를 사이클 길이로 오인한 환산 — 날짜 정보로 정정. 라벨은 사이클 중반(4개월 챔버)~후반 1/3(3개월 챔버)부터 도착 → Model R2R은 **사이클 후반부만 동작** (C8 재정의의 전제). 비율 정의라 데모 스케일 축소에도 불변. 멘토 답변(리드타임) 시 재확인 |
| E7 | 시뮬 라이브 주입·상시 세계 *(신설 2026-07-28 — M3 ②)* | **inject_control**: enabled true · dir `control/inject` · min_interval_sec **1.0** / **persistent_run**: delay_sec **0.01** · limit_wafers **200,000** | M3 ② 월드스테이트 개조 — S11 RUN=상시 세계 주입(파일드롭), STOP=세계 종료. min_interval=디렉토리 스캔 스로틀(주입 지연 상한 ≈1s+1wafer) · limit=데모 1세션 상한(사실상 무제한). 근거 `docs/M3_월드스테이트_개조_v1.md` |

## 결정 기록 *(2026-07-06 — PM 권한 항목만 확정. 나머지는 담당자 확인 후 이 표에 기입)*

| 항목 | 상태 | 비고 |
|------|------|------|
| E1~E5 | ✅ **확정 (PM 윤준호)** | |
| E7 | ✅ **확정 (PM, 신설 2026-07-28)** — M3 ② 커밋과 동시 등재 (헌법 6-1·4-3) | 체인 dry-run 6/6 PASS가 값 검증 |
| E6 | ✅ **확정 (PM, 9차 재정정 2026-07-08)** — **비율 정의**: label_delay = 2개월/PM주기 × cycle (4개월 = 1/2 · 3개월 = 2/3), 절대 환산 ≈ **13,900 wafer** | 처리율 **231 wafer/일** 근거 (3분할 합산 15,919장÷69일=230.7 실측 — 구 170은 train 단독 과소집계). 멘토 답변(리드타임) 시 재확인 |
| B5·B6 (Incident — PM 권한) | ✅ **확정 (PM)** — 병합 20 wafer / reopen 30 wafer | |
| A1~A8 | ✅ **확정 (B 팀원 B, 2026-07-06)** | A4 N=500·A8은 🔧/📊 (E2 확정·B3-1 산출 후 최종화) |
| B1~B4·B7~B9 | ✅ **확정 (B 팀원 B)** | B9 = **P99(1572)로 시작** — PM P99.9 제안은 오발동 실측 후 재검토. B3·B4는 🔧 튜닝 예정 |
| B9-a (anomaly 하한) | ⬜ **미확정 — 값 미등재** *(2026-07-29 기록, C)* | B 회신으로 **눈금**은 확인(ae_score ECDF [0,1]) → 비교는 성립. 다만 **하한값은 미정** — 예측 배선 완료 후 실측과 함께 B(조정 권한)와 확정. 그때까지 SCRAP 판정 보류(fail-safe) |
| LLM 운용 방침 | ✅ **확정 (PM)** — 로컬 LLM, AWS GPU 셀프호스팅 | D9~D14의 전제. 기획서 반영 필요 (헌법 4-3) |
| 정렬 필요 (미결) | ① B4(Drift)↔C6(AE) 신호 주체·용도 정렬 (A·B) | E6는 비율 정의로 재확정 (7차) |
| C1·C2·C5·C6·C9 | ⬜ 제안 — **A(팀원 A) 확인 대기** | |
| C3·C4·C7·C8 | ✅ **멘토 확정 (2026-07-11 멘토링 — 선진행 값 그대로)** | 종결. min_labels_for_auto=50도 회의 컨펌 |
| C2 | ✅ **σ-갭으로 교체 확정 (2026-07-11 회의)** | params.yaml `pass_sigma_gap_max: 3.0` 반영 완료 |
| B 신규 9종 (trim~time_gap + pred_weight) | ✅ **등재 (2026-07-10 PM 승인)** | 출처: B 목록 — spc 섹션. ~~q_low/q_high는 결정 A 구현 착수 시 B 추가~~ → **✅ 등재 (2026-07-12 B)**: q_low 0.00135 / q_high 0.99865 (±3σ 등가 — KEEP\* 분위수 조합②) |
| TTTM 신규 2종 (B4-2) | ✅ **등재 (2026-07-14 PM, 출처: B)** | `tttm_window_wafers: 20` — fleet median rolling window (Incident 병합창 20 정합) / `tttm_window_min_fill: 10` — warmup 최소 충전(⌈W/2⌉), 미충전 챔버 판정 유예. params.yaml·코드는 B 직접 반영 |
| wafer flush 안전망 (D0-2) | ✅ **등재 (2026-07-21 PM, 출처: B)** | `wafer_idle_flush_sec: 30` **[잠정]** — 웨이퍼 완성 tail flush 임계(초). 종료 마커 부재로 "다음 웨이퍼 시작(경계 감지)"이 1차 신호이고, 이 키는 마지막 웨이퍼·유휴 구간용 안전망. dry-run 실측(행間 0.05s·웨이퍼間 1.75s) 대비 안전하나 **실장비 사이클 기준 확정값 재확인 필요**. params.yaml·코드는 B 직접 반영 |
| a-pred 유휴 flush (조인 짝) | ✅ **등재 (2026-08-11, 출처: A 회신 2026-08-10 · B 대행 구현)** | `lean85.idle_flush_sec: 15` — a-pred 가 이 시간 무입력이면 wafer 완료로 간주해 발행. **구 300 은 `consumer.py` env 기본값 하드코딩**이었고(헌법 6-1) 조인 타이밍을 고려한 값이 아니라 버퍼 성장 차단용이었다 → params 로 이관·인하. env `LEAN85_IDLE_FLUSH_SEC` 는 임시 override 전용(설정 시 WARNING). 🔗 **위 `spc.wafer_idle_flush_sec`(30)와 짝** — 지켜야 할 부등식은 설정값(15<30)이 아니라 **유효치**: `idle_flush_sec + IDLE_CHECK_INTERVAL_SEC(5.0) < wafer_idle_flush_sec` (20 < 30, 여유 10s). 26 으로 두면 26<30 이라 통과처럼 보이나 유효치 31 로 **조용히 역전**된다. 어긋남의 증상이 예외가 아니라 **조인율 0%**(양쪽 정상 로그·산출물만 빔)라 기동 시 `consumer._check_idle_flush_ordering()` 이 두 값을 읽어 대조하고 역전이면 ERROR(기동은 미차단) + `tests/test_idle_flush_config.py` 가 부등식을 고정. 근거(A 실측, train_data 111,675 간격): wafer 내 row 간격 p50 3.0s·p99 3.2s·**최대 4.5s**·15s 초과 0건. ⚠️ 부수효과: 중복 발행 차단 기억 TTL = 값 × `FLUSHED_MEMORY_TTL_FACTOR`(2.0) 이라 600s → **30s** |
| incident 신규 2종 (P4-3, PM 권한) | ✅ **등재 (2026-07-13 PM)** | `merge_gap_sec: 300` — 그루퍼 병합축(시간): mock timestamp=wall-clock이라 wafer 창(B5) literal 불가 → dev 시간축, 실물 정합식 ≈ merge_window_wafers×86400/231 (mock 캐던스 실측 후 튜닝). `initial_lifecycle: open` — 신규 incident 초기 상태 (open→analyzing→pending→actioned→verifying→closed) |
| pm_log 경로 1종 (B6-3-f) | ✅ **등재 (2026-07-28, 출처: B)** | `pm_log.path: pm_log.json` — **리포 루트 상대 = 정본**(lean85 README §2 "운영은 루트 최신본"). 요란 판정 확정 시 B(`spc_consumer`)가 여기에 append하고 A(lean-85)는 읽기 전용(계약 §8-B, 단일 작성자). 경로 하드코딩 금지(헌법 6-1)라 키화 — 코드는 `pm_log_writer.resolve_pm_log_path()` 경유. ⚠️ `lean85/pm_log.json` 사본이 생기면 A `find_file`이 루트보다 먼저 집어 split-brain → 기동 시 경고 |
| D1~D6 | ✅ **확정 (C 팀원 C, 2026-07-06)** | |
| D7·D8 | ✅ **확정 (C 벤치 2026-07-09~11)** | **D7 = bge-m3 hybrid**(골든셋 47문항 MRR 0.901 1위, Qwen3-4B 0.887·KORPatent 0.808 탈락) / **D8 = 13,481청크 시딩 완료** (목표 1만 3주 선행). params.yaml 반영 완료 |
| D9·D20·D21 | ✅ **확정 (C 재대결 2026-07-10)** | D9=qwen3:30b-a3b / D20=g5.xlarge 단일 / D21=벤치 Ollama·운영 vLLM 전환 검증만 잔여(C4-3) |
| D9~D22 | ✅ **확정 (D 섹션 회의, 2026-07-07)** — C 제안 초기값 채택 | D7·D8만 C 위임(실측 후). D10~D12·D14·D17~D19·D22는 출발값 — 시뮬 후 조정 원칙 유지 |
| D4-보조 (fast/slow 분기 X값) | ✅ **운영 방침 확정 (PM)** — 출발값 31(분기 비활성) | W6 storm 테스트에서 지연 초과 시 B·C가 score 분포로 재산정 |
| **CT² 인터페이스·자동 배포 (2026-07-30 PM)** | ✅ **등재 (PM 권한 — 헌법 3-3 ③)** — `ct2_auto_promote:false`·`ct2_cmd_cwd`·`ct2_retrain_cmd`·`ct2_validate_cmd` (구 `ct2_auto_deploy` 대체·제거) | 선두 `python`은 `sys.executable` 치환, `{data}/{out}/{bundle}` 치환자. `auto_promote:true`는 헌법 3-3 ③ 개통 조건 5종 충족 전 금지 |
| **CT² 게이트 임계 (2026-07-30 등재 · 2026-08-09 현행화)** | 🔧 **등재 — 전부 「출발값」** (확정 아님) | `ct2_gate_*` **18종**. B: `l5_budget` 0.04(★구 `l5_max` 개명 — 기준 개정 v2 §1. DEFAULTS 는 0.02 유지라 매 판정 `_weakened` 에 잡힌다 = **가드 걸린 완화**)·`recon_rmse_max` 1.0·`calib_mean` 0.05±0.02·`val_p95_max` 0.2 / D: `min_val_wafers` 100·`min_gate_wafers` 100·`anchor_gap_min_ratio` 0.01 / C: `probe_sigma` 4.0·`probe_min_detect` **0.90**(v1 의 0.875 완화 철회 — 가중 채점 도입으로 불요)·`probe_weighted` true(신설 — 부록 A w_c 가중평균)·`probe_sigma_grid` / E: `require_labels` true·`label_min_detect` 0.80·`label_warmup_k` 20·`label_min_wafers` 30 / B5: `chamber_l5_max` 0.05·`chamber_min_wafers` 20. 근거 = REPORT_05 역산 + 기준 개정 v1(8/6)·v2(8/9). 단일 소스 = `validate_bundle.DEFAULTS`(헌법 3-3 ③ ⓑ 위임). **삭제·완화는 PM 승인**. WP-D/WP-4 스윕 재산정 (CI 반폭 축 추가 — 홀드아웃 목표 1000장) |
| **CT² 배포 후 감시 8종 (2026-07-30 WP-C)** | 🔧 **등재 — 「출발값」** (실증 전 스위치 false) | `ct2_watch_*` (enabled·window_wafers 200·min_wafers 100·cross_silence_min_events 10·cross_silence_max 0.5·p95_drop_ratio 0.3·p95_floor 0.02·auto_rollback). 단일 소스 = `ct2_deploy_monitor.WATCH_DEFAULTS`(헌법 3-3 ③ ⓔ). 시뮬 Scorecard 미탐 0건 실증 후 스위치 on |

**후속 조치**: 합의 즉시 `config/params.yaml` 생성 (PM) → 각 모듈은 하드코딩 없이 이 파일만 참조.

**16차 잔여 등재 대기** *(2026-07-30)*: Kafka retention 정책 키(되감기 D9 전제) · `ct2_backfill_hours` 실측 조정값(현 24 근사) · `ct2_gate_*`·`ct2_watch_*` WP-D 스윕 확정값.
